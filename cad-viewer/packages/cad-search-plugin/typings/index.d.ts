/**
 * Hand-maintained public API types for `@hy/cad-search-plugin`.
 *
 * Emitted to `lib/` by `pnpm build:types` (see `scripts/copy-typings.mjs`).
 * Keep in sync with `src/index.ts` exports.
 */
import type { DefineComponent } from 'vue'

export declare const SearchPanel: DefineComponent<object, object, unknown>
