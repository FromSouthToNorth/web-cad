# 性能优化实施计划

> 依据:`渲染与解析性能瓶颈分析.md`(2026-08-20,UTF-8 DXF 焦点,同目录)
> 目标场景:UTF-8 编码 DXF(AC1021+/`$DWGCODEPAGE: UTF-8`,中文 text-heavy),10 万~39 万实体
> 原则:按 ROI 排序、每项独立可回滚(沿用 `_batchFrustumCullingEnabled` 的开关模式)、先基准后改、改后跑差分回归

## 0. 目标与验收指标

| 指标 | 现状(实测) | 目标 |
|---|---|---|
| 打开进度条 | 100% → 0% 重来(大图必现) | 单次打开单调 0→100,阶段文案区分 |
| UTF-8 解析耗时(utf8-mixed-100000) | 843ms / 237MB 峰值堆 | < 750ms / < 210MB |
| GBK 回归(gbk-lines-300000) | 窗口化已生效 | 不劣化(< 5%) |
| 图层开关大图层 | 整索引缓冲重传 | 仅上传变化段(updateRange) |
| 中文多文字图纸打开 | 数千次 MTEXT worker 往返 | 内容级缓存命中复用 |

**M1/M2 复测(2026-08-20)**:

| 指标 | 目标 | 实测 |
|---|---|---|
| 打开进度条 | 单次单调 0→100 | ✅ M1 完成(单测 15/15 + 全量回归) |
| UTF-8 解析耗时 | < 750ms / < 210MB | ✅ 646.9~664.0ms;峰值堆 236MB(耗时达标,堆降幅被转换管线稀释未达 210) |
| GBK 回归 | 不劣化(< 5%) | ✅ 2313ms vs 基准 2428ms(± 噪声,无回归) |

**M3/M4/M5 复测(2026-08-20,当晚定时执行)**:解析基准 `bench-parse` × 三夹具存档于 `realdwg-web/tools/bench/baseline-20260820-m345-post.json`——utf8 703.8ms(两次复测 727→704 噪声带内)、gbk 2243.6ms(优于 M2-post 2313)、ascii 1997.8ms(优于 M2-post 2092);M5-1/M5-2 不进入纯解析路径,差异为本机噪声。渲染/GPU 侧收益(M3 上传量、M4 worker 往返、M5 批内可见性)需真实 GPU 环境集成验证,清单见该 JSON 的 `gpuDependentVerification`。全量回归:three-renderer 53 套 325 测、cad-simple-viewer 74 套 381 测、cad-viewer 5 套 17 测、data-model 112 套 859 测、geometry-engine 34 套 197 测全过;tsc 干净。

**夜间回归(2026-08-21,定时任务第二次执行,确认无退化)**:cad-viewer 侧 132 套 723 测、realdwg-web 侧 165 套 1125 测全过;three-renderer/data-model/geometry-engine tsc 干净。基准复测:utf8 677.5ms(峰值堆 240.7MB)、gbk 2252.1ms(721.8MB)、ascii 2078.7ms(709.4MB),与 M2-post/M345-post 基线均在噪声带内,无退化;`reader-mem-compare` 窗口化 reader 保持 RSS 减半(50.0MB vs 全量解码 98.9MB)。

**夜间回归(2026-08-22,定时任务第三次执行,确认无退化)**:cad-viewer 侧 132 套 723 测、realdwg-web 侧 165 套 1125 测全过;tsc 三包干净。基准复测:utf8 702.2ms(峰值堆 239.7MB)、gbk 2322.0ms(720.3MB)、ascii 2015.3ms(705.9MB),均与基线持平或更优(ascii 优于 M2-post 2092);内存对比维持窗口化 RSS 减半(50.0MB vs 98.8MB)。

**夜间回归(2026-08-23,定时任务第四次执行,确认无退化)**:cad-viewer 侧 132 套 723 测、realdwg-web 侧 165 套 1125 测全过;tsc 三包干净。基准复测:utf8 673.7ms(峰值堆 234.3MB,优于基线)、gbk 2409.9ms(721.2MB,+4.2% 在 ±5% 噪声带内,与历晚 2244~2447 波动一致)、ascii 2122.9ms(706.0MB,+1.5% 噪声);内存对比维持窗口化 RSS 减半(50.5MB vs 99.4MB)。

**夜间回归(2026-08-24,定时任务第五次执行,确认无退化;仓库已含当日 P0/P2/P3/P5 提交 61e618d)**:cad-viewer 侧 132 套 734 测、realdwg-web 侧 167 套 1135 测全过(P 系列新增测试含在内);tsc 三包干净。基准复测:utf8 716.4ms(峰值堆 237.6MB,噪声带内)、gbk 2107.5ms(736.7MB,时间优于基线 2313,堆 +1.8% 噪声)、ascii 1993.9ms(733.1MB,时间优于基线 2092,堆 +3.5% 在噪声带);内存对比维持窗口化 RSS 减半(63.1MB vs 112.4MB)。

**夜间回归(2026-08-25,定时任务第六次执行,确认无退化;仓库当日新增主题切换/图元反选提交 899e645、e28bbd8)**:cad-viewer 侧 133 套 742 测、realdwg-web 侧 167 套 1135 测全过(新功能测试含在内);tsc 三包干净。基准复测:utf8 708.0ms/661.2ms(峰值堆 254.5/236.7MB,首轮堆值偏高经复跑确认为 GC 采样噪声)、gbk 2132.3ms(748.6MB,时间优于基线)、ascii 1941.9ms(710.7MB,时间优于基线);内存对比维持窗口化 RSS 减半(71.7MB vs 120.3MB)。

**夜间回归(2026-08-26,定时任务第七次执行,确认无退化;仓库当日新增 MTEXT 打散为 TEXT 提交 51a0ffb、559dd35)**:cad-viewer 侧 133 套 742 测、realdwg-web 侧 167 套 1135 测全过;tsc 三包干净。基准复测:utf8 667.8ms/656.0ms(峰值堆 250.6/242.2MB,首轮堆值偏高经复跑确认为 GC 采样噪声,复跑回到 236~242 噪声带)、gbk 2011.6ms(726.2MB,时间优于基线 2313)、ascii 1885.6ms(715.0MB,时间优于基线 2092);内存对比维持窗口化 RSS 减半(78.8MB vs 126.6MB)。

**夜间回归(2026-08-27,定时任务第八次执行,修复迁移后回归并确认无退化)**:仓库当日合并 realdwg-web 为 cad-viewer 单一 monorepo(提交 88aa3fa),原 `realdwg-web/` 目录移除,common/data-model/geometry-engine/graphic-interface 迁入 `cad-viewer/packages/` 下。迁移后出现两类 TS 错误导致 86 套件无法启动:
- `@ts-expect-error` 指令在 `AcDbEntity.ts:664/696/726`、`AcDbTable.ts:273/379` 处已无用(tsconfig.jest.json 关闭了 `noUnusedParameters`,原本为 typedoc 保留的未使用参数不再报错),但项目 tsc 仍开启该选项;
- `AcDbEntity.ts:698-706` 的 `@ts-expect-error` 实际掩盖了缺失的 `AcGePoint3dLike`、`AcDbObjectId` 导入(jest 配置关闭 noUnusedParameters 后这些 TS6133 不报,directive 也变 unused)。

修复:`AcDbEntity.ts` 补入 `AcGePoint3dLike` 与 `AcDbObjectId` 导入;`subMoveGripPointsAt` / `subGetOsnapPoints` / `transformBy` / `AcDbTable.numContents` / `AcDbTable.textString` 的未使用参数统一加 `_` 前缀(TypeScript 约定不报 TS6133,typedoc 照常识别);同步移除已无用的 7 处 `@ts-expect-error`。修复后回归:cad-viewer 侧 132 套 / 734 测、data-model/geometry-engine/common 167 套 / 1135 测全过;tsc 三包(data-model/geometry-engine/three-renderer)干净。基准工具已随 realdwg-web 移除(迁移未带走 `tools/bench/`),本轮无法运行 `bench-parse` 与 `reader-mem-compare`;依赖单测覆盖正确性,后续如需基准可在 cad-viewer monorepo 内重建 bench 工具(可参考 baseline-20260820-m345-post.json 的夹具规格)。

**基准工具重建与复测(2026-09-02)**:上述"后续重建"已完成,bench 工具链恢复并可复跑:

- 从 `88aa3fa~1` 恢复原 `realdwg-web/tools/bench/` 全部 21 个脚本与 4 份基线 JSON 至 `cad-viewer/tools/bench/`;`bench-parse.cjs` / `reader-mem-compare.cjs` 增加 monorepo 本地解析回退(`@mlightcad/data-model` 解析失败时直接加载 `packages/data-model/dist/data-model.cjs`),其余脚本原样保留作历史记录。
- 补入原计划 §9 标注"待入仓"的 `generate-utf8-dxf.mjs`(UTF-8/AC1021 + 中文 TEXT/MTEXT 混合,确定性 LCG,流式写出),修正了初版未调用 `stream.end()` 导致 finish 事件不触发的缺陷。
- 夹具重建:gbk/ascii 与历史完全同源(同一生成脚本+同一种子,字节级一致);utf8 夹具原文件未入仓、组合无法还原,按同规格重建(100000 实体,含 15% 中文 TEXT + 7% MTEXT),**utf8 数字不与历史基线直接比较,自本轮起作新基线**。`tools/bench/fixtures/` 已加入 `cad-viewer/.gitignore`(与原仓库不入仓约定一致)。
- 基准复测(best of 3, `--expose-gc`,本机数据模型构建于 2026-09-01):

| 夹具 | 本轮 best / 峰值堆 | 基线(M345-post)/ 历晚波动带 | 结论 |
|---|---|---|---|
| gbk-lines-300000 | 2137.0ms / 717.4MB | 2243.6ms / 719.4MB(历晚 2107~2410) | ✅ 优于基线,无退化 |
| ascii-lines-300000 | 2120.1ms / 730.1MB | 1997.8ms / 709.6MB(历晚 1886~2123) | ✅ 时间在波动带内(顶缘),堆 +2.9% 噪声 |
| utf8-mixed-100000(重建) | 838.0ms / 281.1MB | 不可直接比 | 仅记录,作新基线 |

- `reader-mem-compare`(gbk):窗口化 RSS 101.1MB vs 全量解码 153.9MB,窗口化优势保持(历史口径 50.0 vs 98.9,比例一致;绝对值差来自本机 Node 版本/系统状态,非回归信号)。
- 系统 PATH 非法条目复核:`D:\Android_SDK\Android\Sdk\tools"`(尾部引号)仍存在,修复步骤:Windows 系统属性 → 环境变量 → 将 `Path` 中该条目改为 `D:\Android_SDK\Android\Sdk\tools`(去尾引号)。
- GPU 依赖项未闭环、本机无法推进:见 `baseline-20260820-m345-post.json` 的 `gpuDependentVerification`(M3 上传量、M4 worker 往返/框选计时、M3-4 shader 编译次数、M5-3/M5-4 收益测量),及《性能优化总结.md》§7 第 2/3/4/5(圆弧 LOD)/7 条。M5-3/M5-4 维持"待 GPU profile 数据"的暂缓决策,已决策放弃项(M2-3/M5-1 材质键/P5 strings 合并)维持放弃。
- 全量回归(同日):monorepo 根 jest 332 套 / 2084 测全过(另有 2 套 2 测跳过);`AcTrStyleManager.spec.ts` 在并行全量时因 jest worker 内存耗尽崩溃,单独 `--maxWorkers=1` 重跑 19/19 全过,判定为本机资源限制而非退化。tsc:`data-model` / `geometry-engine` / `three-renderer` 三包 `--noEmit` 全部干净。
- 附带发现:文档 §8.2 记录的 PATH 缺陷在回归期间实际发作——`npx`/`pnpm` 等 cmd shim 无法解析 `node`(报 `'node' is not recognized`)。绕过方式为直接调用 `node node_modules/jest/bin/jest.js`、`node node_modules/typescript/bin/tsc`;根治仍须按上文修复系统 PATH。

## 1. 里程碑总览

| 里程碑 | 内容 | 预估 | 依赖 |
|---|---|---|---|
| **M1** | 进度回退修复(3 处) | 0.5-1 天 | 无 |
| **M2** | UTF-8 解析热路径(值行扫描、编码嗅探、池化实验) | 2-3 天 | 无(顺序实施) |
| **M3** | 渲染缓冲上传与结构小项(4 项) | 1-2 天 | 无 |
| **M4** | MTEXT 字形缓存 + 高亮掩码 dirty-range | 2-3 天 | M2/M3 后 |
| **M5** | 转换管线深度项(traits 缓存、样条 Thomas、大实体分批) | 后续评估 | M4 后按需 |

> 状态:M1 ✅ 完成、M2 ✅ 完成、M3 ✅ 完成、M4 ✅ 完成、M5 ✅ 完成(2026-08-20;M5-1/M5-2 落地,M5-3/M5-4 按数据评估暂缓,M5-1 材质键模板串子项评估后未实施,均见下方实施结果);P0/P2 ✅(2026-08-24,见 §7)、P3/P5 ✅(2026-08-24,见 §7)。

---

## 2. M1 — 进度回退修复

### M1-1 drain 期间数字不再回退(主因)

- **文件**:`cad-viewer/packages/cad-simple-viewer/src/app/AcApOpenFileProgressController.ts:189-231`
- **方案**:`holdUntilSceneIdle` 轮询中删除 `setProgress(drain*100)`(`:213-223`),drain 期间保持 `setProgress(undefined)`(数字隐藏、spinner + "正在渲染"文案,即注释本意)。数字从 100% 消失后不再以低值重新出现。
- **验证**:控制器单测(fake progress + busyGate 恒真 → 断言 `setProgress` 不再被小值调用);手工:>10 万实体图纸 progressive 打开,观察 overlay。
- **风险**:极低;回滚即恢复旧行。
- **收益**:消除用户感知的"进度重来"假象(最高优先级体验问题)。

### M1-2 全局加权分段进度(去掉 FETCH_FILE→CONVERSION 峰值归零)

- **文件**:`AcApOpenFileProgressController.ts:127-137`(normalize)、`:182`(updateOverlay 取整);`cad-simple-viewer/src/app/openFileProgress.ts`(isOpenFileProgressComplete)
- **方案**:
  1. normalize 按域重映射:FETCH_FILE → `pct × 0.10`;CONVERSION → `10 + pct × 0.90`;`_peak` 全局单调,删除 `:130-132` 的归零分支。
  2. `isOpenFileProgressComplete` 改为只判终态(`subStageStatus === 'END' && (subStage === 'END' || stage === 'FETCH_FILE')`,或 ERROR),**不再依赖 percentage ≥ 100**——重映射后 CONVERSION 的尾部 FETCH_FILE END 事件(原 100 现为 10)无法靠数值判定。
  3. 状态栏 `MlProgress.vue` 与 overlay 共用归一化输出,阶段文案随 `subStage` 显示(下载中/解析中/渲染中)。
- **验证**:单测覆盖 normalize 跨阶段单调性;URL 打开场景手工验证(下载 100% 不再归零)。
- **风险**:中——终态判定逻辑是 overlay 隐藏的唯一起点,需全量回归 `isOpenFileProgressComplete` 的调用点(`AcApOpenFileProgressController.updateOverlay`、`MlProgress.vue:59`)。
- **收益**:URL 打开路径的第二次 100→0 消失;进度语义清晰。

### M1-3 失败重开路径保持现状

`onAfterOpenDocument` 失败分支的 `regen()`(`AcApDocManager.ts:1761-1764`)保留,仅在 M1-2 归一化后确认失败重开也单调(新周期从 0 开始是正确语义)。不改代码。

---

## 3. M2 — UTF-8 解析热路径

### M2-1 值行 span 级数值扫描(最大收益项)

- **文件**:`realdwg-web/packages/data-model/src/base/AcDbDxfPairReader.ts`
  - 窗口化 reader `readRaw`(`:447-470`):值行由 `readLine()`(切片)改为 `readLineSpan()` 取 `(start, end)`,仅 string 类值保留切片。
  - 新增 `parseAsciiValueSpan(code, text, start, end)`(`:206-242` 的 span 版):
    - **double**:手写快速路径 `[+-]?digits[.digits][eE[+-]digits]`(覆盖真实 DXF 全部数值形态),失败回退 slice+Number;
    - **int**:span 内跳过空白与尾部垃圾(等价 parseInt 语义),零分配;
    - **long**:同 int + BigInt 罕见回退;
    - **bool/handle**:span 首尾空白检查,无空白则免 trim;
    - **string/binary**:必须保留时再 slice(`decodeHexBinary` 改造为 span 入参)。
  - 同步改造 `acdbMakeAsciiDxfPairReader`(非窗口化路径)保持一致。
- **验证**:
  - 差分测试:对 utf8-mixed-100000、gbk-lines-300000、ascii-lines-300000 三夹具,新旧实现逐 pair 断言值相等(数值严格 `===`);随机 double 模糊测试(1e5 样本含 `1e20/.5/-0/1./E+3` 等边界)。
  - 基准:`bench-parse.cjs` 前后对比 + `reader-mem-compare.cjs` 内存对比。
- **风险**:中——数值语义必须与 `Number()` 完全一致;差分测试是硬门槛。
- **收益**:消除约 70% 值行分配(数值行),预计解析提速 10-20%、峰值堆降 ~10%,GC 卡顿减少。

### M2-2 UTF-8 有效性嗅探兜底(修乱码)

- **文件**:`AcDbDxfPairReader.ts:747-756`(`acdbCreateDxfPairReader` 编码判定)
- **方案**:当判定出的编码非 UTF-8 时,对前 256KB 做**严格 UTF-8 字节序列校验**(零分配字节扫描):若整段合法且含 ≥1 个多字节序列 → 视为 UTF-8 覆盖声明。GBK 中文(尾字节 ≥0x40、常见 0x80-0xBF 续字节在 GBK 中非法)几乎必然校验失败,误判率极低。
- **验证**:新增小夹具(UTF-8 字节 + `$ACADVER AC1015` + `$DWGCODEPAGE ANSI_936`)解析后中文不乱码;gbk-lines-300000 仍按 GBK 解析(回归)。
- **风险**:低;开关可配(默认开)。
- **收益**:修复国内工具"UTF-8 字节 + 陈旧头"图纸的文字乱码。

### M2-3 pair 池化(实验项,若收益 <5% 放弃)

- **事实约束**:`AcDbDxfFiler.readItem` 直接返回 pair 对象(`AcDbDxfFiler.ts:310-321`),`pushBackItem` 依赖对象同一性检测——不能无脑复用对象。
- **方案**:池 + 代数标记——池对象带 `generation`,pushBack 检测改为比较 `(pair, generation)`;回收仅限"未被 pushBack 且消费方同步用完"的 pair(parse 单线程、消费同步是既有不变量)。放在特性开关后,先出原型测收益。
- **验证**:bench 对比 + 全量 DXF 测试套件回归。
- **风险**:中高——别名/生命周期 bug 隐蔽;故放在 M2-1 之后、按收益决定去留。

### M2-4 后台标签页让门优化(低优先级)

- **文件**:`realdwg-web/packages/common/src/AcCmYieldToUi.ts`
- **方案**:`document.hidden` 时用 `setTimeout(0)`/`scheduler.postTask` 替代 rAF(后台 rAF 被节流至 1fps);前台保持 rAF 保证一帧绘制。
- **验证**:后台标签页打开大图计时对比。
- **风险**:低;影响面是全局让门,需跑大图前台路径回归确认无感。

### M2 实施结果(2026-08-20)

**M2-1 值行 span 级数值扫描 ✅ 已落地**

- `AcDbDxfPairReader.ts`:窗口化 + 全文双 reader 值行改 `readLineSpan()`,数值行零切片,999 注释行零切片;`acdbParseDoubleSpan`/`acdbParseIntSpan`/`acdbParseLongSpan` 手写快速路径,double 快速路径在 ≤15 位有效数字且 |指数| ≤ 22 的域内可证明精确(整数 mantissa 精确入 double、10^k 精确可表示、乘除只舍入一次),越域回退 slice+Number;bool/handle/binary 改 span 版,含 `-0` 语义对齐(`parseInt('-')→+0`、`parseInt('-0')→-0`)。
- 验证:`AcDbDxfPairReaderSpan.spec.ts`(10 万随机 double 模糊 × 双 reader + 3 万 int + 3 万 long,严格 `===`);旧 bundle 快照 vs 新 bundle 三夹具逐 pair 差分 567 万 pairs 零差异(`tools/bench/diff-old-new-reader.cjs`);data-model 全量 112 套 853 测全过。
- 收益:reader 纯解析 148→114ms(utf8, **-23%**)、441.7→349.4ms(gbk, **-21%**)、341.4→261.1ms(ascii, **-23.5%**);端到端 best:utf8 731.9→664.0ms(-9.3%)、gbk 2428.0→2281.5ms(-6.0%)、ascii 2188.1→2092.1ms(-4.4%);utf8 峰值堆 245.4→236.0MB(转换管线对象占大头,稀释了 reader 层收益)。

**M2-2 UTF-8 有效性嗅探 ✅ 已落地**

- 新增 `acdbValidateUtf8Prefix(bytes, 256KB)` 严格 RFC 3629 校验(过短/代理区/超长/4 字节边界按规范拒绝,窗口边界允许跨窗完成尾序列);仅当自动探测到旧代码页 + 全前缀合法 + 含 ≥1 多字节序列时覆盖为 UTF-8;`sniffUtf8: false` 或显式 `options.encoding` 可关闭。
- 验证:`AcDbDxfUtf8Sniff.spec.ts` 15 测全过(AC1015+ANSI_936 陈旧头实为 UTF-8 → 中文正确解码;真实 GBK 夹具回归不受影响);三夹具旧新差分仍零差异;gbk 端到端复测 2313ms 无回归。
- 已知边界:若 GBK 字节流恰好在整段内构成合法 UTF-8 序列(如"中文" GBK 字节 D6D0 CEC4)会误判——真实 GBK 文件 256KB 前缀内出现 <0xC2 前导或 <0x80 尾字节的概率接近必然,风险可忽略。

**M2-3 pair 池化 ❌ 放弃(有数据)**

- `tools/bench/pool-ceiling.cjs` 原型:64 槽环形池"理想版"(忽略 filer pushBack/`_lastRead` 引用保留的真实风险)在 utf8 夹具反而慢 6.8%、gbk 慢 5.3%——V8 新生代 bump 分配极快,复用可变对象破坏对象形状与逃逸分析,上限为负。
- 结论:不再实现(免去 filer 同一性协议改造的中高风险);原型脚本保留作决策记录。

**M2-4 后台标签页让门 ✅ 已落地**

- `AcCmYieldToUi.ts`:`accmYieldToUi`/`accmYieldForPaint` 在 `document.hidden` 时改用 `setTimeout(0)`(后台 rAF 被节流至 ~1fps 会拖慢协作式解析),前台保持 rAF 保证每片后出帧。
- 验证:`AcCmYieldToUi.spec.ts` 7 测全过,common 全量 18 套 60 测全过;后台标签大图计时需浏览器环境,留待集成验证。

---

## 4. M3 — 渲染缓冲与结构小项

### M3-1 可见性切换局部上传

- **文件**:`cad-viewer/packages/three-renderer/src/batch/drawVisibility/AcTrIndexedBatchDrawVisibilityStrategy.ts:25-52`
- **方案**:collapse/restore 修改索引段后,补 `indexAttr.addUpdateRange(indexStart, indexCount)` 再 `needsUpdate = true`(three.js 对 index 属性同样走 `bufferSubData`);快照复制保留(恢复必需)。
- **验证**:`AcTrBatchDrawVisibility.spec.ts` 扩展断言 updateRange;手工切大图层记录帧时间/上传量。
- **风险**:低。
- **收益**:图层开关不再整缓冲(MB 级)重传。

### M3-2 optimize 尾部清零段局部上传

- **文件**:`AcTrBatchedLine.ts:557-564`(清零段裸 `needsUpdate`);顺带审计 Mesh/Point/Line2 三个批类同类路径。
- **方案**:清零段加 `addUpdateRange`。
- **验证**:批量删除后 `renderer.info` 上传量对比。
- **风险**:低。

### M3-3 free-list 去排序

- **文件**:`AcTrBatchedMixin.ts:223-243`(`reserveGeometryId` 每次 `sort + shift`)
- **方案**:改为单调栈/最小堆,O(1)/O(log n) 取空闲槽,删除时压入。
- **验证**:`AcTrBatchedGroupBulkRemove.spec.ts` 回归 + 大删后连续新增的微基准。
- **风险**:低。

### M3-4 disposeObject 共享材质保护

- **文件**:`cad-viewer/packages/three-renderer/src/object/AcTrEntity.ts:247-295`;`AcTrBatchedGroup.addLine`(批量路径共享材质处)
- **方案**:批量路径把共享材质标记(如 `material.userData.acTrShared = true`),`disposeObject` 增加该保护条件,跳过 dispose。
- **验证**:打开大图后观察 shader 编译次数(首帧不再因 program 重建卡顿)。
- **风险**:低——只增保护条件,不改变 dispose 逻辑本身。

### M3 实施结果(2026-08-20)

四项全部落地,three-renderer 全量 50 套 303 测全过、tsc 干净:

1. **M3-1 可见性切换局部上传 ✅**:`AcTrIndexedBatchDrawVisibilityStrategy.ts` collapse/restore 在 `needsUpdate` 前补 `indexAttr.addUpdateRange(indexStart, indexCount)`(restore 用 `info.indexStart + snapshot.length`);快照复制保留。测试扩展 `AcTrBatchDrawVisibility.spec.ts`:新增"仅标记折叠索引段"用例并断言 `updateRanges === [{start, count}]`(含 indexStart=3 的偏移段)。图层开关不再整索引缓冲重传,只走 `bufferSubData` 变化段。
2. **M3-2 optimize 尾部清零段局部上传 ✅**:`AcTrBatchedLine.optimize` 与 `AcTrBatchedMesh.optimize` 尾部清零段加 `addUpdateRange(nextIndexStart, length - nextIndexStart)`;`AcTrBatchedPoint` 无索引缓冲、搬移段本已带 range,无需改动;`AcTrBatchedLine2.optimize` 尾部清零改 `packed.fill(0, clearStart)`,并在 instanceStart/instanceEnd 共享的 InterleavedBuffer 上加 `addUpdateRange`(非 interleaved 回退路径保持整段 `needsUpdate`,该路径实际不可达)。批量删除后尾部清零只上传变化段。
3. **M3-3 free-list 去排序 ✅**:`AcTrBatchedMixin.ts` 新增 `pushAvailableGeometryId`/`popAvailableGeometryId` 二叉最小堆,`reserveGeometryId` 改为堆顶取最小 id、`deleteGeometryById` 改为堆插入;外部语义与旧实现完全一致(最小 id 优先复用)。新增 `AcTrBatchedMixinFreeList.spec.ts` 6 测(乱序压入弹出升序、交错压弹、单元素、1000 元素随机负载与全排序期望一致、删除→复用、空表追加),`AcTrBatchedGroupBulkRemove.spec.ts` 回归通过。大删后连续新增从 O(n² log n) 降至 O(n log n)。
4. **M3-4 disposeObject 共享材质保护 ✅**:`AcTrMaterialManager.createMaterial` 统一给缓存材质打 `metadata.isShared = true`(`AcTrMaterialMetadata` 新增可选字段,BackSide variant、layer-bound 材质均经此路径自动继承标记);`AcTrEntity.disposeObject` 第三步对 `isShared` 材质跳过 dispose 与纹理释放。新增 `AcTrEntity.spec.ts` disposeObject 3 测(共享跳过、私有正常 dispose、多材质数组只跳过共享项)。转换期 shell 销毁不再连坐共享样式材质,避免 program 重建;未标记的私有材质(预览/图像等)行为不变。GPU 侧 shader 编译次数对比需真实 GPU 环境,留待集成验证。

---

## 5. M4 — MTEXT 字形缓存与高亮掩码

### M4-1 MTEXT 内容级字形缓存

- **文件**:`cad-viewer/packages/three-renderer/src/renderer/AcTrMTextRenderer.ts:148-156` + `AcTrMText.ts`(注:`@mlightcad/mtext-renderer` 为外部依赖,缓存落在本包)
- **方案**:`Map<key, GlyphTree>` 缓存 worker 排版结果,key = (文本内容, 样式键, 颜色键);命中后仅做矩阵放置(克隆/变换),跳过 worker 往返。LRU 上限(如 512 条)+ 估算字节数;字体加载完成时失效对应条目;特性开关默认开。
- **验证**:构造 1 万条重复内容 MTEXT 的夹具,对比打开耗时与 worker 任务数;`AcTrMText.spec.ts`/`AcTrMTextRenderer.spec.ts` 回归。
- **风险**:中——缓存内存增长与失效时机;加 LRU 与开关兜底。
- **收益**:中文多标签图纸打开显著提速;`_pendingGeometryJobs` 更快清零 → overlay 提前结束,间接缓解进度观感。

### M4-2 高亮掩码 dirty-range 重建

- **文件**:`cad-viewer/packages/three-renderer/src/batch/highlight/AcTrBatchHighlightState.ts:194-230`
- **方案**:CPU 侧记录 min/max 脏槽,`uploadMaskTexture` 只重写该区间(总带宽从"全槽数"降至"变化槽数");GPU 侧 `needsUpdate` 仍为整张上传(three.js DataTexture 无 updateRange API)——如需进一步,在 onBeforeRender 用 `gl.texSubImage2D` 局部上传(单独评估)。
- **验证**:框选 10 万对象计时;`AcTrBatchHighlightShaders.spec.ts` 回归。
- **风险**:低-中。

### M4 实施结果(2026-08-20)

**M4-1 MTEXT 内容级字形缓存 ✅ 已落地**

- 新增 `three-renderer/src/renderer/AcTrMTextGlyphCache.ts`:LRU 模板缓存(`Map<key, MTextObject>`,默认 512 条 / 16MB 估算字节上限,超限逐出最久未用);`buildKey` 键 = (文本内容, 除 position 外的全部布局字段, textStyle, colorSettings)——position 有意排除(位置在根变换上,一个模板服务所有插入点)。
- 关键实现细节:`clonePlacedMTextTemplate` 深克隆模板并重定位根节点;THREE `Object3D.copy` 会把 userData 走 JSON 克隆、破坏渲染器 layout 元数据里 Box3 的原型,克隆后按树同步**引用恢复** `layout/lineLayouts/logicalBounds/layoutCache`(已验证 mtext-renderer 的 `buildCharBoxesFromObject` 是 copy-then-transform,引用共享是只读安全的);叶子 BufferGeometry 逐件 clone,防止批量路径 rebase-in-place 与 shell dispose 污染共享模板;`box` 深拷贝到克隆。
- 采用**两次命中晋升**策略避免反噬:key 首次出现返回原渲染对象(独有文本零克隆开销),第二次出现时才把新渲染结果存入缓存并从此返回重定位克隆,模板始终保持 pristine。缓存默认开,`setContentGlyphCacheEnabled(false)` 可整体关闭(清空缓存);字体加载完成(`FontManager.events.fontLoaded`,initialize 时订阅一次)、`setFontUrl`、`setDefaultFonts`、`dispose` 均失效缓存。
- 验证:新增 `AcTrMTextGlyphCache.spec.ts` 10 测(key 排除 position/区分内容样式颜色、LRU 逐出、字节预算逐出、克隆重定位、几何隔离、Box3 原型恢复、box 拷贝、消费者变异不污染模板)+ `AcTrMTextRendererGlyphCache.spec.ts` 5 测(两击晋升、sync 路径同享、独有内容不缓存、开关旁路、字体加载失效);three-renderer 全量 53 套 325 测全过、tsc 干净。浏览器端 1 万条重复 MTEXT 的 worker 往返数与打开耗时对比需集成验证。

**M4-2 高亮掩码 dirty-range 重建 ✅ 已落地**

- `AcTrBatchHighlightState` 新增 `_dirtyRangeStart/_dirtyRangeEnd` 与持久化 `_maskData` 像素缓冲;`setHighlight`/`clearSlot` 扩展脏区间、`clearAll` 标记全区间;`uploadMaskTexture` 只在脏窗口内重写像素,布局变化(尺寸增长)时重分配缓冲并全量重写,`force` 仍全量。GPU 侧保持整张 `needsUpdate`(DataTexture 无 updateRange),收益在 CPU:框选/悬停单槽变更不再重建 O(宽×高×4) 的全尺寸数组。
- 验证:新增 `AcTrBatchHighlightState.spec.ts` 7 测(首传全量、增量只动脏窗口、清除像素归零、缓冲复用、布局增长重分配全量、force 全量、clearAll 全零);`AcTrBatchHighlightShaders.spec.ts`/`AcTrBatchedGroupHighlight.spec.ts`/`AcTrBatchedGroupBulkRemove.spec.ts` 回归全过。框选 10 万对象计时需浏览器集成验证。

---

## 6. M5 — 转换管线深度项(后续,按数据决定)

1. **worldDraw traits 瘦身**:`lineStyle` 按线型记录缓存单例(`AcDbEntity.ts:901-915` 每次分配新对象)、`resolvedColor` 解析期快照、材质键模板串复用 —— 预期 drain 降 1-3s。
2. **样条 Thomas O(n)**:`geometry-engine/src/util/AcGeNurbsUtil.ts` 稠密高斯消元改三对角/带状求解 —— 消除单条样条数百 ms 长任务。
3. **大 INSERT/hatch/ACIS 分批化**:单实体长任务切片,让门可抢占。
4. **槽级视锥剔除**:批级已落地,槽级为后续增益项。

### M5 实施结果(2026-08-20)

**M5-1 worldDraw traits 瘦身 ✅ 已落地(2/3 子项)**

- **lineStyle 按线型记录缓存单例**:`AcDbEntity.ts` 新增模块级 `WeakMap<AcDbLinetypeTableRecord, {version, styles: Map<type, AcGiLineStyle>}>` 组成样式缓存;`lineStyle` getter 按 (记录, ByLayer/ByBlock/UserSpecified) 复用同一对象,不再每次 worldDraw 做 `{type, ...linetype}` 展开分配(389k 实体 ≈ 78 万次对象分配 → 每 (记录×类型) 一次)。`AcDbLinetypeTableRecord` 新增 `linetypeVersion` 计数(setAttr 重写自增),记录重定义时缓存自动重建;`pattern` 数组保持引用共享,原地编辑仍即时可见。消费方对 traits.lineType 只读(渲染器 buildKey/读取),共享安全。
- **resolvedColor ByBlock 快照**:ByBlock 无 INSERT 所有者的前景色(ACI 7)改为类级单例 `_byBlockForegroundColor`,不再每次 draw 分配新 `AcCmColor`(与 ByLayer 路径返回共享图层色对象的既有约定一致,消费方只读)。
- **材质键模板串复用:未实施(有评估)**。`AcTrLineMaterialManager.buildKey` 每实体一次模板串拼接,单次开销为亚微秒级,远小于本项已消除的对象分配;增加键缓存层需在每次 worldDraw 变异 traits 的复用对象上做失效跟踪,复杂度与收益不成比例。若后续 profile 显示字符串拼接成为热点可再做。
- 验证:新增 `AcDbEntity.spec.ts` 6 测(重复访问同对象、跨实体共享、ByLayer/UserSpecified 区分、记录重定义重建、pattern 原地编辑可见、ByBlock 前景色单例);data-model 全量 112 套 859 测全过、tsc 干净。

**M5-2 样条带状求解 ✅ 已落地**

- `AcGeNurbsUtil.ts`:稠密 O(n³) 高斯消元替换为稀疏带状求解器 `solveBandedLinearSystem`(行按起始切线→插值行→结束切线重排,带宽 degree+1;消元含 fill-in 处理,枢轴搜索覆盖全部剩余行以容纳行交换位移;单次分解同时求解 X/Y/Z 三右端)。`acgeInterpolateNurbsCurve` 矩阵构建同时改为 span 定位(`findKnotSpan` 二分)+ 支撑窗口内的基函数求值,去掉了 O(n²) 的稠密矩阵与全行求值。复杂度 O(n³) → O(n²·band);实测 n=800 求解约 200ms(测试环境)。
- 验证:新增 `AcGeNurbsBandedSolver.spec.ts` 5 测——内置旧稠密实现作参考,随机输入(无切线/有切线/Chord 参数化)逐点差分 1e-8 一致、插值性质保持(参数点回代精度 1e-6)、大系统性能上限;`AcGeNurbsUtil.spec.ts` 全过(含 start+end 切线用例,该用例暴露了稀疏消元 fill-in 缺失导致伪奇异的问题,已修复)。geometry-engine 全量回归通过。

**M5-3 大 INSERT/hatch/ACIS 分批化:评估后暂缓(记录)**

- 现状复核:`AcDbRenderingCache` 已具备模板压实与实例化(`compacted clones share geometry buffers`,README 与 `AcTrGroup.fastDeepClone` 支撑),重复 INSERT 已从 O(实体) 克隆降至 O(材质) 克隆;同步 draw 管线内的剩余长任务(未压缩块 fastDeepClone、hatch 三角化、ACIS 线框解码)要切片需把 `worldDraw/subWorldDraw` 全链路改为可恢复的异步迭代,属计划预估中的"后续评估"级改造。
- 决策依据:本机无 GPU 环境无法测量分批收益;既有渲染缓存已消解最高频的克隆成本;改造面跨 data-model/three-renderer 两包且触及块渲染正确性核心。按计划纪律(收益为负或风险过高按数据放弃)暂缓,待真实 GPU 环境 profile 数据后决定。未修改代码。

**M5-4 槽级视锥剔除:评估后暂缓(记录)**

- 现状复核:批级剔除(`AcTrBatchedMixin.syncFrustumBounds`)已落地;槽级剔除需要在一次 draw call 内跳过中间槽,three.js 常规 `WebGLRenderer` 对普通 `BufferGeometry` 无 multi-draw 支持,拆 drawRange 多段绘制则把单批次拆回多次 draw call,与批处理目标相悖。
- 决策依据:收益依赖"单批内槽级空间散布 + 大量被遮挡槽"的场景,本机软件渲染环境无法验证;可行方案(自定义 indirect/multi-draw 渲染路径或按槽分组重建批)风险高、ROI 未证实。暂缓,待 GPU 环境用 `renderer.info` 收集批内可见槽占比数据后决定。未修改代码。

---

## 7. P0/P2/P3/P5 — 剩余热点落地(2026-08-24)

依据 2026-08-24 源码级复评,落地两项剩余热点:

### P0 圆/弧零对象密度 ✅

- **文件**:`realdwg-web/packages/geometry-engine/src/geometry/AcGeCircArc3d.ts`、`AcGeEllipseArc3d.ts`(新增 `getPointsFlat(numPoints, out?)`);`cad-viewer/packages/three-renderer/src/object/AcTrLineGeometryBuilder.ts`(新增 `buildFlatLineGeometryMulti`,`buildLineGeometryMulti` 接受 Float64Array);`AcTrRenderer.ts`、`AcTrLine.ts`。
- **方案**:`circularArc`/`ellipticalArc` 由 `getPoints(100)`(每弧 101 个 AcGePoint3d + 数组)改为 `getPointsFlat(100)`(单个交错 xyz Float64Array,与 getPoints 逐点位级相等);lineStrip payload / `buildLineGeometryMulti` / `AcTrLine` 接受 `AcGePoint3dLike[] | Float64Array`。flat 构建器常规路径(无细分/拆分)直接单趟写入打包几何;步长超 5e5 或跨度超 1e6 的巨型弧回退对象管线,细分与重基拆分行为逐位一致。椭圆额外消除 minorAxis getter 每点 clone 的分配。
- **验证**:`AcGeArcGetPointsFlat.spec.ts` 4 测(圆/弧/椭圆与 getPoints 逐点 `===`、out 复用);`AcTrLineGeometryBuilder.spec.ts` 新增 4 测(常规圆与对象管线逐位一致、巨型圆回退一致、fat 路径、空输入);`AcTrRendererDirectLineCapture.spec.ts` 新增 2 测(圆/椭圆捕获为 Float64Array payload)。微基准:2 万圆密度垃圾峰值 3.2MB → 1.2MB(约 3 倍垃圾量下降)。
- **风险**:低;位级一致性由测试锁定。

### P2 drain 阈值裁剪 ✅

- **文件**:`realdwg-web/packages/data-model/src/base/AcDbDxfPairWire.ts`(`trimToCount` + `MAX_TRIM_SLACK_RATIO = 1.25`)。
- **方案**:`acdbDrainDxfPairs` 末尾不再无条件 `.slice(0, count)` 全量拷贝;容量超出用量 ≤25% 时返回 subarray 视图(零拷贝,transfer 本按整 buffer 传输),大幅超配才精确裁剪。
- **验证**:`AcDbDxfPairWire.spec.ts` 新增 2 测(小余量保持零拷贝视图、大幅超配精确裁剪)。
- **风险**:低;length/count 语义不变。

### P3 splitLineSegmentsClusters 预分配 TypedArray ✅

- **文件**:`cad-viewer/packages/three-renderer/src/object/AcTrLineGeometryBuilder.ts`。
- **方案**:三段式改造——预扫描计数后,细分顶点写入预分配 `Float64Array`(顶点数 >65535 时索引用 `Uint32Array`);聚类阶段所有 cluster 为同一对共享缓冲的 subarray 视图,精确长度、零 per-cluster 分配、零 `Float64Array.from`/`new Uint16Array` 拷贝。行为与旧 number[] 实现逐位一致(尺寸标注/图案填充/代理图形命中)。
- **验证**:`AcTrLineGeometryBuilder.spec.ts` 新增 3 测(单簇精确内容、退化 (0,0) 跳过、跨 1e6 拆分 + 共享 buffer 零拷贝契约);`AcTrSpanningSplit`/`AcTrLineSegments` 回归全过。
- **风险**:低;拆分/细分阈值语义不变。

### P5 解析路径小项 ✅

- **文件**:`realdwg-web/packages/data-model/src/base/AcDbDxfKeyword.ts`(新增 `acdbDxfKeywordUpper`)+ 6 个调用文件。
- **方案**:
  1. code-0 关键字/实体名/段名的 `toUpperCase()` 全部改为 `acdbDxfKeywordUpper`:纯 ASCII 大写返回原字符串引用(零分配);含 ASCII 小写或任何非 ASCII 字符时精确回退 `toUpperCase()`,与旧行为完全等价。覆盖 `AcDbDxfDocumentReader`(13 处)、`AcDbDxfEntityFactory`(2 处,每实体)、`AcDbDxfFiler.registerHandle`(每 handle)、`AcDbDxfHeaderReader`/`AcDbDxfObjectsReader`/`AcDbDxfAcdsDataReader`/`AcDbDxfPolylineAssembler`。
  2. binary DXF `readString` 的每串 `new TextDecoder(encoding)` 提升为 reader 级单实例复用(`AcDbDxfPairReader.ts`)。
- **未实施(有评估)**:strings 表合并单池——回放时按偏移切片会让主线程每个字符串值都产生分配,劣于现状的共享引用,放弃。
- **验证**:`AcDbDxfKeyword.spec.ts` 4 测(大写同引用、ASCII 小写/混合转换、非 ASCII 与 `toUpperCase` 逐字相等);data-model 全量回归。
- **风险**:低;等价性由"非 ASCII 必回退"保证。

### 回归与基准(2026-08-24)

- realdwg-web data-model+geometry-engine 串行全量 149 套 1075 测全过(含 Filer 改动后 data-model 114 套 874 测复跑);cad-viewer three-renderer 53 套 334 测全过;tsc:data-model(build)/three-renderer 干净。
- bench-parse utf8-mixed-100000 复测两次:best 655.6~704.2ms / 峰值堆 237.6MB(一次 279.8MB 为机器负载噪声,复测回落),落在历史基线带内,无回归;P5-c 在主线程解析路径上,小写检查零分配生效。

---

## 8. 执行顺序与依赖

```
M1(0.5-1d) ─┐
             ├─→ M3(1-2d) ─┐
M2-1(1-2d) ──┘             ├─→ M4(2-3d) ─→ 复测 → M5 评估
M2-2(0.5d, 同文件随后) ────┘
M2-3(实验) / M2-4(低优) 可并行穿插
```

M1 与 M2-1 互不依赖可并行;M2-2 与 M2-1 同文件,顺序实施;M3 各项独立小改可批量;M4-1 依赖 M2 完成后重测基座。

## 9. 测试与基准纪律

- **改动前**先跑一次全量基准存档:`bench-parse.cjs` × {utf8-mixed-100000, gbk-lines-300000, ascii-lines-300000} × `--repeat 3 --gc` + `reader-mem-compare.cjs`。
- **每项改动**跑对应 jest 套件(data-model `AcDbDxfWindowedReader.spec.ts`、three-renderer batch 系列 spec、cad-simple-viewer 新增 normalize 单测)。
- **每里程碑**跑三夹具差分(数值逐 pair 相等)与交互回归清单(打开大图、缩放平移、图层开关、框选 10 万、批量删除)。
- **回滚**:M2-2/M2-3/M4-1 均挂特性开关,开关默认值在里程碑复测后决定;M1/M3 为小 diff 可直接 revert。
- 基准夹具:`cad-viewer/tools/bench/fixtures/`(gbk/ascii 由 `generate-gbk-dxf.mjs` / `generate-large-dxf.mjs` 重建;utf8 由 2026-09-02 入仓的 `generate-utf8-dxf.mjs` 生成)。
