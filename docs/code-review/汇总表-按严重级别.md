# 代码评审问题汇总表（去重 · 按严重级别）

> 来源：`docs/code-review/raw/*.json`（14 领域 109 条 finding，confirmed 104 条）。
> 本表已剔除 5 条被对抗验证推翻的误报，并合并 5 处同根因重复（`AcApDocManager.destroy()` ×3 域、`_pendingGlyphKeys` ×2 域、`openDocument` 重入 ×2 域、`OrbitControls` 未销毁 ×2 域）。
> 去重后共 99 条：P0 0 条、P1 14 条、P2 38 条、P3 47 条。

## 是否阻塞合并的判定口径

- **是**：会导致 CI/构建失败；或崩溃、数据损坏、默认路径下结果错误；或在 38 万实体默认场景下造成秒级冻结/不可用。
- **否**：需要非默认触发条件（超大单批、反复创建销毁、异常输入、冷路径导出等），或影响限于告警/可维护性。

## P1（14 条）

| # | 问题 | 文件 | 行号 | 严重级别 | 证据 | 修复建议 | 是否阻塞合并 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| P1-1 | grip 拖拽预览每次 mousemove 深克隆实体并重建 transient 几何 | `cad-viewer/packages/cad-simple-viewer/src/editor/grip/AcEdGripPreviewJig.ts` | 43-53, 64-68 | P1 | `this._view.removeTransientEntity(this._previewEntity.objectId) ... const preview = this._sourceClone.clone() ... preview.subMoveGripPointsAt([this._gripIndex], this.compu` | 构造时克隆一次，mousemove 只更新几何/矩阵（或改走 view.createEntityPreview + updateEntityPreview，如 AcEdBatchPreview 的做法）；用 rAF 合并同一帧内多次 mousemove；并对异步发布做“是否仍为当前预览”的校验，避免被取代的 transient 落入场景。 | 否 |
| P1-2 | updateEntity 调 batchConvert 未配平待处理计数，导致 _numOfEntitiesToProcess 下溢并被钳到 0 | `cad-viewer/packages/cad-simple-viewer/src/view/AcTrView2d.ts` | 2047-2049, 2986-2992, 3165-3172 | P1 | `void (async () => { await this.batchConvert(entities)  …  if (epoch === this._convertEpoch) { this.decreaseNumOfEntitiesToProcess()  …  this._numOfEntitiesToProcess-- if ` | 在 updateEntity 调 batchConvert 前做同样的计数配平（_numOfEntitiesToProcess/_totalEntitiesToProcess += entities.length），或改为由调用方统一管理计数、batchConvert 不再自减。 | 否 |
| P1-3 | clear() 不重置每文档布局状态：第二个文档复用上一张图纸的布局视图、相机与首次访问标记 | `cad-viewer/packages/cad-simple-viewer/src/view/AcTrView2d.ts` | 2219-2235, 2413-2415, 655-658 | P1 | `clear() { this._convertEpoch++ this._convertQueue.length = 0  …  let layoutView = this._layoutViewManager.getAt(layoutBtrId) if (layoutView == null) {  …  const isFirstVi` | clear() 中清空 _layoutViewManager（为 AcTrLayoutViewManager 增加 clear() 并重建）、_initializedLayouts、_externallyFramedLayouts、_loadingLayouts、_convertingLayers，并按新文档重新创建布局视图与视口视图。 | **是** |
| P1-4 | 事务变更记录器对每条变更做全数组线性扫描，批量删除/编辑退化为 O(N²)，38 万实体规模下主线程冻结 | `cad-viewer/packages/data-model/src/database/transaction/AcDbChangeRecorder.ts` | 55-68、79-83、127-140、196-218、228-239（调用点 database/AcDbBlockTableRecord.ts:499-504、database/ | P1 | `return this.changes.findIndex( change => change.kind === kind && change.object.objectId === objectId && acdbAreChangeContainersEqual(change.container, container) )` | 为 modify / sysvar / 结构变更各维护 Map<objectId, changeIndex> 或 Set<containerKey+objectId> 去重索引，替代 find/findIndex；结构取消配对改为按 key 查表。 | **是** |
| P1-5 | 批量 entityAppended/entityErased 通知使用 push(...items)，单批超过 V8 参数上限时抛 RangeError（撤销大范围删除即触发） | `cad-viewer/packages/data-model/src/database/AcDbDatabase.ts` | 828-838、845-855 | P1 | `if (this.isEventBatched()) { const items = Array.isArray(entity) ? entity : [entity] this._pendingEntityAppended.push(...items) return }` | 改为 for 循环 push 或按固定块（如 1000）分批 push（与 endEventBatchChunked 一致）；undo/redo 的实体恢复通知同样分块派发。 | **是** |
| P1-6 | AcGeCircArc3d.deltaAngle 对整圆返回 0，CIRCLE 的最近点/切点/垂足捕捉全部失效 | `cad-viewer/packages/geometry-engine/src/geometry/AcGeCircArc3d.ts` | 215-217（消费点 344、402、440） | P1 | `get deltaAngle() { return AcGeMathUtil.normalizeAngle(this.endAngle - this.startAngle) } // nearestPoint: if (t > delta) t = delta` | 改为 get deltaAngle() { return this.closed ? TAU : AcGeMathUtil.normalizeAngle(this.endAngle - this.startAngle) }，或在 nearestPoint/tangentPoints/perpendicularPoints 内用 closed 短路（closed 时 t 直接落在 | **是** |
| P1-7 | AcApDocManager.destroy() 不销毁视图：rAF 循环永不停、document.body 上的 Stats DOM 与已实现的 teardown 入口全部零调用 | `cad-viewer/packages/cad-simple-viewer/src/app/AcApDocManager.ts` | 625-632 | P1 | `AcTrView2d.ts:2365 this._rafId = requestAnimationFrame(this.animate)（自续，永不停）；:2295 stopAnimationLoop() { cancelAnimationFrame(this._rafId) } 全仓 0 调用点；:2344 document.body.` | AcTrView2d 新增 dispose()：stopAnimationLoop()、移除 document keydown / canvas / layoutManager.layoutSwitched / AcDbSysVarManager / AcApSettingManager 监听、stats.dom.remove()、_renderer.dispose()、_ho | 否 |
| P1-8 | 进程级单例 AcDbSysVarManager 上的监听无退订句柄，且 AcApDocument.destroy() 不清库，旧数据库（38 万实体）被闭包长期持有并响应 SETVAR | `cad-viewer/packages/cad-simple-viewer/src/app/AcApContext.ts` | 116-138 | P1 | `AcDbSysVarManager.instance().events.sysVarChanged.addEventListener(args => { ... if (view.hasSceneContent) { view.clear(); args.database.regen() } })（AcApContext 全文 0 处 r` | AcApContext/AcTrView2d 保存每个监听句柄并提供 dispose()，由 AcApDocManager.destroy() 调用；AcApDocument.destroy() 增加 database 释放（clear()）。 | 否 |
| P1-9 | AcSvgEntity.fastDeepClone() 返回 this，违反 AcDbRenderingCache 模板不可变契约：重复 INSERT 变换叠加，且导出后污染共享缓存 | `cad-viewer/packages/cad-svg-plugin/src/AcSvgEntity.ts` | 147-149（配合 126-133 applyMatrix；调用方 cad-viewer/packages/cad-svg-plugin/src/AcSvgRenderer.ts | P1 | `fastDeepClone() { return this }   // 而 applyMatrix: else { this._matrix = matrix.clone().multiply(this._matrix) }` | 实现真正的 fastDeepClone（新建 AcSvgEntity，复制 _localSvg、_box、_matrix、basePoint），并让 AcSvgRenderer.prepareExport() 在导出开始与结束都调用 AcDbRenderingCache.instance.clear()（或在缓存 key 中加入渲染器身份）以避免污染 three 渲染路径。 | **是** |
| P1-10 | agent draw_arc 把“度”直接传给按弧度解释的 AcDbArc，画出的圆弧角度错误 | `cad-viewer/packages/cad-agent-plugin/src/tools/CadActionExecutor.ts` | 291-301 | P1 | `const arc = new AcDbArc( toPoint3d(input.center), input.radius, input.startAngleDeg, input.endAngleDeg )` | 改为 degToRad(input.startAngleDeg)/degToRad(input.endAngleDeg)，或把入参改名并同步修改 cadTools.ts 的 description 与字段名。 | **是** |
| P1-11 | CI 的 lint 门禁在 HEAD 上已失败：4 个包 21 个 error，build job 必然红 | `cad-viewer/.github/workflows/ci.yml` | 48-49 | P1 | `- name: Lint code run: pnpm lint` | 在 cad-viewer 下跑 pnpm lint:fix 修掉 14 个 import 排序错误；手工把 7 处动态 t(...${id}) 改为静态 key（或对动态 key 处加 eslint-disable 并说明理由）；删除 package.json:81 的 --config ./.eslintrc.js。建议 CI 增加 lint:fix 后 git diff - | **是** |
| P1-12 | 每实体热路径用 Object3D.getObjectById 反查批次容器（O(子树) 线性扫描） | `cad-viewer/packages/three-renderer/src/batch/AcTrBatchedGroup.ts` | 1257（applyBatchSlotVisibility，被 addEntity:1015/1028/1041/1050 与 registerDirectAppend:1257  | P1 | `private applyBatchSlotVisibility(...) { const batchedObject = this.getObjectById( item.batchedObjectId ) as AcTrBatchedObject \| undefined batchedObject?.setVisibleAt(ite` | 在 AcTrBatchedGroup 内维护 _containerById: Map<number, AcTrOriginBatch>（add(batch) 时登记、clear/dispose 时删除），所有 getObjectById(item.batchedObjectId) 改为查表；或让 addLine/addLine2/addMesh/addPoint 返回 cont | **是** |
| P1-13 | AcTrMTextRenderer._pendingGlyphKeys 永不回收，dispose() 也未解绑 fontLoaded 监听 | `cad-viewer/packages/three-renderer/src/renderer/AcTrMTextRenderer.ts` | 76, 451-464, 484-500 | P1 | `const occurrences = (this._pendingGlyphKeys.get(key) ?? 0) + 1 if (occurrences === 1) { this._pendingGlyphKeys.set(key, 1) return rendered }` | 把 _pendingGlyphKeys 改为 Set 并在消费后清理，或让其复用 _glyphCache 的预算（记录 occurrence 时同步淘汰）；新增 clearDocumentState() 由 AcTrRenderer.dispose()/AcTrView2d.clear() 调用以清空 _pendingGlyphKeys 与 _glyphCache；dispos | 否 |
| P1-14 | 属性面板为全部选中实体构建运行时属性树，实际只用第一个 | `cad-viewer/packages/cad-viewer-example/src/shell/panels/AntdPropertiesPanel.vue` | 93-102 | P1 | `selectionSet.value.forEach(id => { const entity = db.tables.blockTable.modelSpace.getIdAt(id); if (entity) list.push(entity.properties) })；仅 list[0] 被消费（:106 entityPropsL` | 只解析第一个实体：const id = selectionSet.value[0]; const entity = id != null ? modelSpace.getIdAt(id) : undefined，属性树只建一次；多选数量用 selectionSet.value.length；若后续要展示多选公共属性，先按类型分桶再抽样。 | **是** |

## P2（38 条）

| # | 问题 | 文件 | 行号 | 严重级别 | 证据 | 修复建议 | 是否阻塞合并 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| P2-1 | openDocument/openUrl 无重入（in-flight）保护，第二次打开会与第一次交错解析同一个 AcDbDatabase 与同一个 converter 实例 | `cad-viewer/packages/cad-simple-viewer/src/app/AcApDocManager.ts` | 923-944 | P2 | `this.onBeforeOpenDocument(options) try { await this._openFileProgress.beginOpen(this.context.doc.database)` | 在 AcApDocManager 内加 _openInFlight?: Promise<boolean> 守卫：进行中时直接拒绝/排队第二次 open（返回 false 或 await 前一次），或提供 abort 语义取消前一次；同时把 OPEN 命令的 active 生命周期延伸到文件选择+读取完成（例如让 open-file 事件返回 promise 并由命令 await | 否 |
| P2-2 | 命令输入期每次 mousemove 触发 12~15 次 getBoundingClientRect（强制同步布局） | `cad-viewer/packages/cad-simple-viewer/src/editor/input/ui/AcEdFloatingMessage.ts` | 257-276（另见 AcEdRubberBand.ts:92-94,209-213,303,337） | P2 | `protected setPosition(pos: AcGePoint2dLike) { const hostRect = this.host.getBoundingClientRect() const rect = this.container.getBoundingClientRect()` | 缓存 canvas/container 的 rect（viewResize 时失效）并把布局读取集中到 rAF 一帧一次；橡皮筋/浮层用单个 transform 写入替代多次 rect 读取。 | 否 |
| P2-3 | OSNAP 标记每次 mousemove 销毁并新建 DOM 节点 | `cad-viewer/packages/cad-simple-viewer/src/editor/input/ui/AcEdFloatingInput.ts` | 355-372（另见 AcEdOSnapMarkerManager.ts:37-51,69-72、AcEdMarker.ts:58,109） | P2 | `this.osnapMarkerManager.hideMarker() ... this.lastOsnapPoint = this.view.osnapResolver.resolve({ cursorWcs: wcsPos, ... }) ... this.osnapMarkerManager.showMarker(this.las` | 每个 manager 复用一个 AcEdMarker 实例，mousemove 只 setPosition（必要时用已有的 set type 切换形状），无捕捉点时才 hide。 | 否 |
| P2-4 | OSNAP 每个候选实体重复执行一次 mask→模式数组转换 | `cad-viewer/packages/cad-simple-viewer/src/editor/input/AcEdOsnapResolver.ts` | 140-158 | P2 | `const modes = acdbMaskToOsnapModes(AcApSettingManager.instance.osnapModes) modes.forEach(mode => this.collectOsnapPointsByMode(entity, mode, ...))` | 在 collectOsnapPoints 入口把 acdbMaskToOsnapModes 结果算一次并向下传参；命中最高优先级模式后提前结束遍历（或先按实体/模式优先级排序）。 | 否 |
| P2-5 | 子索引构建用逐条 insert 而非接口自述的批量 load() | `cad-viewer/packages/cad-simple-viewer/src/spatialIndex/AcTrHierarchicalSpatialIndex.ts` | 352-364 | P2 | `const existing = this.childIndexes.get(id) if (existing) { existing.clear() finiteItems.forEach(item => existing.insert({ ...item }))  …  finiteItems.forEach(item => spat` | 两条 forEach 改为 spatialIndex.load(finiteItems)（新索引为空树，正是 load 的最优场景）；已存在的子索引 clear() 后同样 load()，并去掉 { ...item } 拷贝。 | 否 |
| P2-6 | 代理图形 PushMatrix 使用 Float64Array 视图，遇到 4 字节对齐 chunk 抛 RangeError，载荷不足 128 字节时静默读入后续 chunk | `cad-viewer/packages/data-model/src/misc/proxyGraphic/AcDbProxyGraphic.ts` | 728-733 | P2 | `private pushMatrix(data: Uint8Array) { const values = new Float64Array(data.buffer, data.byteOffset, 16)` | 改用 new DataView(data.buffer, data.byteOffset, Math.min(128, data.length)) 循环 getFloat64，或先校验 data.length>=128 再复制到 8 字节对齐的临时缓冲。 | 否 |
| P2-7 | AcDbLayerTableRecord 的 isFrozen/isLocked setter 置 false 时无法清除标志位（解冻/解锁无效） | `cad-viewer/packages/data-model/src/database/AcDbLayerTableRecord.ts` | 188-194、256-262 | P2 | `set isFrozen(value: boolean) { const flag = value ? 1 : 0 this.standardFlags = this.standardFlags \| flag }` | this.standardFlags = value ? this.standardFlags \| 1 : this.standardFlags & ~1（isLocked 同理用 0x04）。 | 否 |
| P2-8 | LWPOLYLINE 凸度段固定 100 采样且每采样点两次对象分配，热点圆弧 LOD 未覆盖多段线 | `cad-viewer/packages/data-model/src/entity/AcDbPolyline.ts` | 649-670 | P2 | `：650 const centerline = this._geo.getPoints(100)；665-668 centerline.forEach(point => points.push(new AcGePoint3d().set(point.x, point.y, this.elevation)))；AcGePolyline2d.` | 为 AcGePolyline2d/AcGeCircArc2d 增加长度感知的 flat 采样（镜像 getPointsFlat(out?: Float64Array)），LWPOLYLINE 复用渲染器已有的 arcLodDiagonal/arcSegmentCount 计算每段采样数，并让 subWorldDraw 直接产出 Float64Array 供 renderer.l | 否 |
| P2-9 | LWPOLYLINE 挤出方向（DXF 210）只存储不生效，镜像/异面多段线几何错误 | `cad-viewer/packages/data-model/src/entity/AcDbPolyline.ts` | 853-873 | P2 | `：853-873 case 210: nx = n ... this.normal.set(nx, ny, nz).normalize()（仅赋值 _normal）；:649-670 subWorldDraw 只做 this._geo.getPoints(100) 并把 z 设为 this.elevation，全函数未引用 this._n` | 在 AcDbPolyline.dxfInFields 结束处按 AcDbArc.applyDxfInGeometry 的做法用 refVec 把每个顶点 OCS→WCS 写回（或保留 normal 并在 subWorldDraw/geometricExtents/snap 统一用挤出矩阵变换），并补 LWPOLYLINE/2dPolyline 210 用例。 | 否 |
| P2-10 | TABLE 行/列数直接来自文件并用作数组长度，异常输入可致整份图纸打开失败 | `cad-viewer/packages/data-model/src/entity/AcDbTable.ts` | 339-347 | P2 | `：344-346 this._rowHeight = new Array<number>(rows).fill(0) / this._columnWidth = new Array<number>(cols).fill(0) / this._cells = new Array<AcDbTableCell>(rows * cols)；:11` | 在 resizeGrid 内校验 Number.isInteger(rows) && rows>0 && rows<=MAX（并对 rows*cols 做乘法溢出/上限判断，超出则退化为按需扩容或按 cellIndex 上限裁剪），同时给 readEntitiesSection 的单实体解析加 try/catch，坏实体计入 unknownEntityCount 后继续。 | 否 |
| P2-11 | 图层缺失时每个实体每次绘制触发一次 console.error | `cad-viewer/packages/data-model/src/entity/AcDbEntity.ts` | 1029-1039 | P2 | `：1032-1034 console.error(\The layer with name '${this.layer}' not found in drawing database!\)；调用链 :214 const layerColor = this.getLayerColor() ← resolveStandardColor ← :` | 在数据库或实体上缓存“已确认缺失的图层名”集合，首次命中时 console.warn 一次并返回 undefined（配合打开结束时的汇总告警），后续静默走 fallback 颜色。 | 否 |
| P2-12 | AcGeCircArc2d 三点构造用斜率倒数求圆心，弦轴对齐时圆心/半径变 NaN | `cad-viewer/packages/geometry-engine/src/geometry/AcGeCircArc2d.ts` | 102-130（内部唯一调用点 433） | P2 | `const slope = (p1, p2): number => (p2.y - p1.y) / (p2.x - p1.x) const perpSlope = (m: number): number => -1 / m const x = (b2 - b1) / (m1 - m2)` | 改用向量法求外接圆：对两条弦求垂直平分线并解 2x2 线性方程组（或复用 AcGeCircArc3d.computeCenterPoint 的做法），并在求交前用弦长平方下界剔除退化弦，避免出现 1/m 与 ∞·0。 | 否 |
| P2-13 | AcGeEllipseArc3d.closed 语义相反：整椭圆 false、零扫掠 true | `cad-viewer/packages/geometry-engine/src/geometry/AcGeEllipseArc3d.ts` | 358-360（对照 144-151） | P2 | `get closed() { return this.deltaAngle == 0 }` | 统一为“整圈”语义，如 get closed() { return this.deltaAngle === TAU \|\| Math.abs(this.deltaAngle - TAU) < 1e-10 }，并显式处理零扫掠（不可当作整椭圆）；随后可移除 AcDbEllipse.ts:374 的补偿判断。 | 否 |
| P2-14 | NURBS 逐点采样走 evaluate()：每点重建控制点数组并计算一二阶导数 | `cad-viewer/packages/geometry-engine/src/geometry/AcGeNurbsCurve.ts` | 112-134（热路径 AcGeSpline3d.ts:521-539，内层 AcGeNurbsUtil.ts:647-668） | P2 | `point(u: number): number[] { return this.evaluate(u).point } // evaluate(u) { const controlPointsArray = this._controlPoints.map(p => [p.x, p.y, p.z]); return acgeEvaluat` | 在 AcGeNurbsCurve 上缓存数值化的控制点数组（构造/变更时失效），点采样改用 acgeEvaluateNurbsPoint（或新增加只算位置的 evaluatePoint），并把基函数循环限制在非零区间 [span-p, span]，避免为取点付 deriv1/deriv2 的代价。 | 否 |
| P2-15 | 布局视图永不回收：每个 AcTrLayoutView/AcTrViewportView 都向共享 canvas 挂一个 OrbitControls（+document keydown capture），无 disconnect 路径 | `cad-viewer/packages/cad-simple-viewer/src/view/AcTrView2d.ts` | 2413-2429 | P2 | `AcTrBaseView.ts:288 const cameraControls = new OrbitControls(this._camera.internalCamera, this._renderer.domElement)；AcTrLayoutViewManager.ts:30 private _layoutViews: Map` | AcTrBaseView 增加 dispose()（_cameraControls.dispose()）；AcTrLayoutViewManager 增加 clear()/delete(btrId) 并遍历视图 dispose；AcTrView2d.clear()/bindDrawDatabase 时按新文档 btrId 重建 _layoutViewManager；VPORT  | 否 |
| P2-16 | AcSvgRenderer.image() 返回共享哑对象 _tempEntity，真实 image 实体被推迟到微任务追加：栅格图恒被画在最上层且丢失块/INSERT 变换 | `cad-viewer/packages/cad-svg-plugin/src/AcSvgRenderer.ts` | 293-301（返回值定义见 377） | P2 | `const pending = AcSvgImage.fromBlob(blob, style, traits, ctx).then(entity => this.pushEntity(entity) ) this._pendingImages.push(pending.then(() => undefined)) return _tem` | 让 image() 返回一个可被 applyMatrix 的真实占位/延迟实体（或同步返回 AcSvgImage 并让 dataUrl 稍后填充），按插入位置保序追加而非仅 push 到末尾；并捕获 fromBlob 的 rejection 以免污染整个导出。 | 否 |
| P2-17 | HTML 快照 buildAsync 的让出点失效：osnap 目录构建整块同步执行，单图层大图纸只让出一次 | `cad-viewer/packages/cad-html-plugin/src/AcApHtmlSnapshotBuilder.ts` | 130-154（141 让出、149-151 同步构建 osnap） | P2 | `const collected = collectBatchesFromObject3D(layer.internalObject) ... await accmYieldForPaint()   // 仅每图层 ... osnap: shouldExportOsnap(options) ? buildOsnapCatalog(datab` | 在 buildOsnapCatalog 的实体遍历中按批次调用 accmYieldForPaint（或将其移入可让出的分片调度），并把 osnap 构建放到图层几何收集之后单独让出。 | 否 |
| P2-18 | PDF 导入 closePath 守卫条件错误：同一 path 内第 2 个及以后的子路径丢失闭合段 | `cad-viewer/packages/cad-pdf-plugin/src/AcApPdfImportConvertor.ts` | 180-186 | P2 | `case OPS.closePath: { if (current.length > 0 && subpaths.length === 0) { current.push({ ...current[0] }) } flush()` | 条件改为 if (current.length > 0) { current.push({ ...current[0] }) }，并避免首点重复入顶点（在 subpathToEntity 里判重）。 | 否 |
| P2-19 | 批次几何导出用 number[] 逐浮点 push，未按已知 vertexCount 预分配 | `cad-viewer/packages/cad-html-plugin/src/AcExSceneBatchCollector.ts` | 208-227（同类问题另见 262-282 exportActiveBatchedLine2Slice） | P2 | `const activeFloats: number[] = [] ... for (let i = 0; i < floatCount; i++) { activeFloats.push(positionArray[start + i]!) } return { positions: new Float32Array(activeFlo` | 先遍历 active 槽位求和（info.vertexCount 已知），一次性 new Float32Array(total) 并用游标写入，或复用 copyFloat32Range 的写游标模式；wide-line 分支直接读 instanceStart/instanceEnd 的底层 array。 | 否 |
| P2-20 | 快照 base64 编解码逐字符拼接整段载荷字符串 | `cad-viewer/packages/cad-html-plugin/src/AcExSnapshotCodec.ts` | 58-73 | P2 | `let binary = '' for (let i = 0; i < bytes.length; i++) { binary += String.fromCharCode(bytes[i]!) } return btoa(binary)` | 改为分块处理：每次取 8192 字节用 String.fromCharCode.apply 或 TextDecoder 风格的 chunk 拼接，或直接用 fflate 的 strToU8/strFromU8 配合分片 btoa；导出侧可考虑改用 Blob+URL 而不是内联 base64。 | 否 |
| P2-21 | SVG/PDF 导出把整张图纸拼成单个字符串，峰值约为文档体积的 2-3 倍；PDF 还要额外构建整棵 DOM | `cad-viewer/packages/cad-svg-plugin/src/AcSvgRenderer.ts` | 315-362（PDF 侧 cad-viewer/packages/cad-pdf-plugin/src/AcApPdfConvertor.ts:56-78） | P2 | `for (const entity of this._entities) { const svg = entity.renderSvg(); if (svg) { parts.push(svg); ... } } const elements = parts.join('\n') ... ${elements}` | 导出改为流式/分块：分批 renderSvg 后写入 Blob 分片数组（new Blob(parts) 支持多段而不必 join），PDF 侧先用 DocumentFragment/innerHTML 增量解析或分页转换；确需整体字符串时明确设上限并给出进度。 | 否 |
| P2-22 | 导出 HTML 运行时永久清空几何 CPU 数组，WebGL 上下文丢失后无法恢复 | `cad-viewer/packages/cad-html-plugin/src/AcExViewerMemory.ts` | 117-124（调用点 cad-viewer/packages/cad-html-plugin/src/AcExHtmlViewerRuntime.ts:626） | P2 | `const bufferAttr = candidate as unknown as THREE.BufferAttribute bufferAttr.array = new Float32Array(0) as typeof bufferAttr.array bufferAttr.clearUpdateRanges() bufferAt` | 保留可重建的最小数据（或记录可回填的标记），在 renderer.domElement 上监听 webglcontextlost/restored，恢复时从快照批次重新构建 BufferGeometry；若确要保持现状，至少在上下文恢复回调里给出提示而非静默空白。 | 否 |
| P2-23 | 智能体验证截图对全部模型空间实体做同步预览提取，单条消息最多重复 5 次 | `cad-viewer/packages/cad-agent-plugin/src/agent/drawingPreviewCapture.ts` | 17-21, 53-54 | P2 | `export function getModelSpaceEntityIds(): AcDbObjectId[] { const modelSpace = AcApDocManager.instance.curDocument.database.tables.blockTable.modelSpace return [...modelSp` | 不要用全量 model-space id：优先复用当前视图 canvas 截图，或只取本轮工具创建/修改的实体 id（draw_* 已返回 entityIds），必要时对 id 分批并 accmYieldForPaint 让出主线程。 | 否 |
| P2-24 | 设置面板重算标签碰撞时不按文档要求的优先级排序，任何面板改动都可能改变显示的名称集合 | `cad-viewer/packages/cad-tunnel-plugin/src/applyTunnelSettings.ts` | 91-102 | P2 | `const keep = update.labelCollision ? selectNonOverlapping(boxes) : boxes.map(() => true)` | 在 Text 实体上持久化标签优先级（XDATA）或维护 名称→优先级 映射后排序；或在 updateTunnelEntities 中只处理高度/线宽变化，碰撞显隐沿用绘制时记录的 visibility 与优先级。 | 否 |
| P2-25 | eslint 的 vue-i18n localeDir 匹配不到任何文件，no-unused-keys 静默失效 | `cad-viewer/eslint.config.js` | 81 | P2 | `localeDir: './**/locales/*.{json,json5,ts,js,yaml,yml}',` | 把 pattern 改成与实际结构一致，例如 localeDir: './src/locale/*/*.ts'（多个包可用数组/对象形式），并补一条冒烟测试（保留一个只定义未使用的 key，断言 lint 报错）来防止再次静默失效。 | 否 |
| P2-26 | changeset fixed 组漏掉 5 个已发布包，文档要求的 lockstep 版本无法达成 | `cad-viewer/.changeset/config.json` | 5-19 | P2 | `"fixed": [ [ "@hy/cad-agent-plugin", ... "@hy/three-renderer" ] ],` | 把这 5 个包并入同一个 fixed 组（需先把它们的 version 对齐到统一版本线），并把 access 改为 public；或明确放弃 lockstep，同步修改 CLAUDE.md:73 的约定。 | 否 |
| P2-27 | tools/release.mjs 不校验 tag 版本与各包 version 是否一致，也不执行 changeset version | `cad-viewer/tools/release.mjs` | 109-112 | P2 | `run(git tag -a ${tag} -m "release: release ${tag}"); run(git push origin ${tag});` | 打 tag 前读取所有 workspace 包的 version，与目标版本比对，不一致直接 fail 并提示先跑 pnpm changeset version；或由 release.mjs 直接调用 changeset version 后再校验 git status --porcelain 为空才继续。 | 否 |
| P2-28 | cad-invertsel-plugin 对 data-model 用 semver 范围而非 workspace:*，是全仓唯一漂移且永不被同步检查 | `cad-viewer/packages/cad-invertsel-plugin/package.json` | 48,52 | P2 | `"@hy/data-model": "^1.13.0" }, "peerDependencies": { "@hy/cad-simple-viewer": "workspace:*", "@hy/data-model": "^1.13.0"` | 把两处都改为 workspace:*；并考虑在 sync-versions.mjs 中把内部包名（@hy/*）纳入强制 workspace: 协议检查，避免同类漂移再次出现。 | 否 |
| P2-29 | splitLineSegmentsClusters 输出索引用 Uint16Array，单簇段数 >32767 时静默截断 | `cad-viewer/packages/three-renderer/src/object/AcTrLineGeometryBuilder.ts` | 830、916-917（对比 776-779 已按 subVertCount 选 Uint32） | P2 | `const outIndices = new Uint16Array(totalSegments * 2) ... const l1 = (outArrayPos - clusterArrayStart) / itemSize - 1 ... outIndices[outIdxPos++] = l1 outIndices[outIdxPo` | 让 outIndices 与 AcTrSegmentCluster.indices、buildLineSegmentsGeometry 的 indices 形参一起支持 Uint32Array（按簇内顶点数 >65535 选择，与 776-779 一致），或对每个簇附加段数上限（如 32767 段即强制 pushCluster/startCluster）。 | 否 |
| P2-30 | 高亮掩码 hasAnyHighlight 每次 draw call 全量扫描 mask | `cad-viewer/packages/three-renderer/src/batch/highlight/AcTrBatchHighlightState.ts` | 217-224（调用点 AcTrBatchHighlightShaders.ts:533 的 onBeforeRender；另 AcTrBatchedMixin.ts:1150） | P2 | `hasAnyHighlight() { for (let i = 0; i < this.selectedMask.length; i++) { if (this.selectedMask[i] \|\| this.hoveredMask[i]) { return true } } return false }` | 用计数替代扫描：在 setHighlight/clearSlot/clearAll 中维护 highlightCount（或每槽位的 selected/hovered 计数），hasAnyHighlight() 返回 count>0；批量清除时置 0。 | 否 |
| P2-31 | 批次 dispose 不释放高亮掩码 DataTexture（GPU 纹理泄漏） | `cad-viewer/packages/three-renderer/src/batch/AcTrBatchedMixin.ts` | 1270-1274（AcTrBatchHighlightState.ts:60 maskTexture，全文无 dispose 方法） | P2 | `dispose() { this.userData.batchDisposed = true this.geometry.dispose() return this }` | AcTrBatchedMixin.dispose() 中加 this._highlightState.dispose()（内部 this.maskTexture?.dispose(); this.maskTexture=null; 清空掩码与 dirty/pending 状态），并在 AcTrBatchedGroup.clear() 的批次 dispose 路径复用。 | 否 |
| P2-32 | resetGeometry 会复活已删除（inactive）的槽位 | `cad-viewer/packages/three-renderer/src/batch/AcTrBatchedLine.ts` | 265-276（getUserData）、287-314（resetGeometry） | P2 | `getUserData() { const userData: AcTrBatchGeometryUserData[] = [] const geometryInfoList = this._geometryInfo geometryInfoList.forEach(item => { userData.push({ position: ` | getUserData() 中跳过 !isBatchGeometryActive(item.flags) 的记录（或在 resetGeometry 里只遍历 active 槽），必要时在 optimize 末尾把尾部连续 inactive 记录截断。 | 否 |
| P2-33 | 每个 POINT 实体重复创建点符号几何并再次 clone 入批 | `cad-viewer/packages/three-renderer/src/object/AcTrPoint.ts` | 32-41、51-52（几何来源 geometry/AcTrPointSymbolCreator.ts:453-480） | P2 | `const pointSymbol = AcTrPointSymbolCreator.instance.create(style.displayMode) ... const geometry = pointSymbol.point ?? new THREE.BufferGeometry().setFromPoints([_vector3` | 在 AcTrPointSymbolCreator 内为每个 displayMode 缓存一份不可变模板（point 与 line 各一），AcTrPoint 只引用共享模板；入批时让 addGeometry/addPoint 直接从源数组拷贝到批次缓冲（或允许传入只读模板 + 内部复制），避免每实体 clone。 | 否 |
| P2-34 | AcTrRenderer.dispose() 不解绑 FontManager 单例监听，也不释放注入的 WebGLRenderer | `cad-viewer/packages/three-renderer/src/renderer/AcTrRenderer.ts` | 147-152, 667-670 | P2 | `dispose() { this._context.styleManager.dispose() FontManager.instance.missedFonts = {} }` | 构造时用命名函数（或封装返回 off 句柄）保存监听引用，dispose() 里对两个事件 removeEventListener；同时 dispose() 内调用 this._renderer.dispose()（按需 forceContextLoss()），明确 WebGLRenderer 生命周期归属 AcTrRenderer。 | 否 |
| P2-35 | QuickSelect 每个下拉字段都在主线程全量扫描 modelSpace 并逐实体建对象 | `cad-viewer/packages/cad-viewer/src/composable/useQuickSelect.ts` | 196-222 | P2 | `const iterator = modelSpace.newIterator()\n  for (const entity of iterator) {\n    result.push({\n      id: entity.objectId,\n      entity\n    })\n  }（:214-220）` | 在 useQuickSelect 内按 (docId, applyTo, objectType) 缓存一次 collectSourceItems 结果，documentActivated 时失效；sourceCount 用缓存长度；matchedCount 复用同一份 items 调 filter.matches，不再调 selectAll；objectType/value 候 | 否 |
| P2-36 | useSystemVars 在进程级 sysVarChanged 单例上注册监听且从不注销 | `cad-viewer/packages/cad-viewer/src/composable/useSystemVars.ts` | 150-186 | P2 | `AcDbSysVarManager.instance().events.sysVarChanged.addEventListener(args => {…}) … editor.events.documentActivated.addEventListener(args => { reset(args.doc.database) })；全` | 把两个监听提为具名 handler 并在 onScopeDispose 中 removeEventListener（与 useLayerFilters.ts:322 一致）；或改为模块级单次注册 + isInitialized 守卫（如 useDark.ensureColorThemeSync）。 | 否 |
| P2-37 | useLayouts 在全局 layoutManager 单例上注册匿名 layoutSwitched 监听且从不注销 | `cad-viewer/packages/cad-viewer/src/composable/useLayouts.ts` | 35-51 | P2 | `acdbHostApplicationServices().layoutManager.events.layoutSwitched.addEventListener(args => { … reactiveLayouts.forEach(layout => {…}) })；文件内无 onScopeDispose` | 保存具名 handler，onScopeDispose 中 removeEventListener（editor.events.documentActivated 与 layoutManager.events.layoutSwitched 都要），与 useLayers 的清理方式对齐。 | 否 |
| P2-38 | 示例壳层的选择/文档监听在组件卸载时不注销 | `cad-viewer/packages/cad-viewer-example/src/shell/ribbon/AntdPropertyBar.vue` | 159-188 | P2 | `events.selectionAdded.addEventListener(onSelectionAdded)\n    events.selectionRemoved.addEventListener(onSelectionRemoved)\n  … onUnmounted(stopSelectionBindTimer)` | 把 events 与 handler 保存下来，在 onUnmounted 中成对 removeEventListener（selectionAdded/selectionRemoved/documentActivated），并把 selectionBound/documentBound 与移除逻辑成对维护。 | 否 |

## P3（47 条）

| # | 问题 | 文件 | 行号 | 严重级别 | 证据 | 修复建议 | 是否阻塞合并 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| P3-1 | AcApOpenFileProfiler.waitForIdle 的 16ms 轮询无上限、cancel() 无法中断，且会让下一次 open 的 profile 被上一次的轮询提前结束 | `cad-viewer/packages/cad-simple-viewer/src/app/AcApOpenFileProfiler.ts` | 221-237 | P3 | `await new Promise<void>(resolve => { const poll = () => { if (!view.isProcessingEntities) { resolve() return } setTimeout(poll, 16)` | 给轮询加会话标识与上限：begin() 生成 epoch/AbortSignal，poll 每轮先检查 epoch 是否仍匹配、并累加最大等待（如 120s 后放弃并 publish），cancel() 置位标志让 poll 立刻退出；publishReport 前再校验 epoch。 | 否 |
| P3-2 | CLI 内置静态服务器 readFile 失败无 catch：unhandled rejection（Node 默认抛出/退出）且响应悬挂 | `cad-viewer/packages/cad-simple-viewer-cli/src/runHeadless.ts` | 258-261 | P3 | `void readFile(filePath).then(body => { res.writeHead(200) res.end(body) })` | 改为 await readFile 并用 try/catch 返回 500，或至少补 .catch(error => { res.writeHead(500); res.end(String(error)) })；同时在 :243 用 statSync().isFile() 区分目录。 | 否 |
| P3-3 | AcApPluginManager.registerLazyPlugin 触发器冲突时抛出异常但保留半注册状态，导致该插件永久无法再注册/加载 | `cad-viewer/packages/cad-simple-viewer/src/plugin/AcApPluginManager.ts` | 136-151 | P3 | `this._lazyRegistrations.set(pluginName, registration) for (const trigger of registration.triggers) { const normalizedTrigger = trigger.trim().toUpperCase() ... if (this._` | 先做完整校验（构造临时 trigger→plugin 映射，检查空值/重复/与既有映射冲突），全部通过后再一次性提交 _lazyRegistrations.set 与 _triggerToPluginName 的批量写入；或在 catch 中回滚已写入的 trigger。 | 否 |
| P3-4 | 公开导出的 i18n cmdDescription 未小写化命令名，查表必然未命中并返回原始 key | `cad-viewer/packages/cad-simple-viewer/src/i18n/index.ts` | 38-41 | P3 | `export const cmdDescription = (groupName: string, cmdName: string) => { const key = command.${groupName}.${cmdName} return AcApI18n.t(key)` | 让自由函数委托 AcApI18n.cmdKey（或至少 cmdName.toLowerCase()），消除两套实现；若命令名大小写敏感是有意契约，则应在 doc 注释与 AcApI18n 侧统一并补测试。 | 否 |
| P3-5 | CLI 静态服务器目录前缀校验可被 ../ 绕过（path.join 归一化后仍满足 startsWith(root)） | `cad-viewer/packages/cad-simple-viewer-cli/src/runHeadless.ts` | 235-241 | P3 | `const filePath = path.join(root, relative) if (!filePath.startsWith(root)) { res.writeHead(403)` | 改用 path.relative(root, filePath) 并判断结果不以 '..' 开头且非绝对路径（或用 filePath === root \|\| filePath.startsWith(root + path.sep)）。 | 否 |
| P3-6 | preloadDefaultFonts 的 fire-and-forget 调用吞掉字体加载 rejection，产生未处理 Promise 拒绝 | `cad-viewer/packages/cad-simple-viewer/src/app/AcApDocManager.ts` | 564-566 | P3 | `if (options.preloadDefaultFonts) { void this.loadDefaultFonts() }` | 改为 this.loadDefaultFonts().catch(error => log.warn('[AcApDocManager] Preload fonts failed', error))。 | 否 |
| P3-7 | 顶点标记 points.push(...cached) 按单实体顶点数展开，可抛 RangeError | `cad-viewer/packages/cad-simple-viewer/src/editor/marker/AcEdSelectionVertexMarkers.ts` | 147-157 | P3 | `const entity = blockTable.getEntityById(id) cached = entity ? entity.subGetGripPoints() : [] this._pointsCache.set(id, cached) } points.push(...cached)` | 改为 for (const p of cached) points.push(p) 或 points = points.concat(cached)；对总点数设上限（超出跳过或降采样），避免展开调用与一次性巨型缓冲。 | 否 |
| P3-8 | AcApDrawStyleToolbar.dispose() 无调用点，destroy 后泄漏 capture 监听与 DOM | `cad-viewer/packages/cad-simple-viewer/src/ui/AcApDrawStyleToolbar.ts` | 403-421（另见 :374,:387-390,:394、app/AcApDocManager.ts:512,625-632） | P3 | `dispose(): void { this.hideColorPanel() document.removeEventListener( 'pointerdown', this.onDocumentPointerDown, true )` | 在 AcApDocManager.destroy() 中调用 this._drawStyleToolbar.dispose()（并把视图/编辑器析构统一串起来）；注意公开 getter drawStyleToolbar 允许宿主自行调用，因此该 dispose 目前只是“可能被外部调用”。 | 否 |
| P3-9 | loadLayoutEntitiesIfNeeded 的异步转换失败被吞掉（含整布局数组展开入队），计数永不回减 | `cad-viewer/packages/cad-simple-viewer/src/view/AcTrView2d.ts` | 2541-2558 | P3 | `this._numOfEntitiesToProcess += entities.length const convert = async () => { try { if (this._progressiveRendering) { this._convertQueue.push(...entities)  …  void conver` | 改为分批入队（for 循环 push 或 splice 分批），并给 convert 加 catch/回滚：记录 pending 数，失败时一次性扣回 _numOfEntitiesToProcess，同时避免未处理 rejection。 | 否 |
| P3-10 | 层次索引 window 模式对每个命中重复做全量子项遍历与分配 | `cad-viewer/packages/cad-simple-viewer/src/spatialIndex/AcTrHierarchicalSpatialIndex.ts` | 196-207, 218-233 | P3 | `const level2 = child.search(bbox) const hitEx: AcEdSpatialQueryResultItemEx = { ...hit, children: level2 }  …  const allChildren = child.all().filter(isFiniteSpatialBBox)` | 一次遍历完成判定：取 all() 一次后在循环里同时判相交与全包含（避免 search + all 两遍），并去掉 { ...hit } 复制（把 children 原地挂到命中对象上）；或把 options 透传给 child.search 并比较子项总数以判定全包含。 | 否 |
| P3-11 | 渐进式让帧：每个实体一次 async 调用 + await（38.9 万次 promise 与微任务） | `cad-viewer/packages/cad-simple-viewer/src/view/AcTrView2d.ts` | 2998-3005 | P3 | `if (yieldGate) { // Yield for input/overlay, but do not force a full-scene paint here — const didYield = await yieldGate.maybeYield(yieldToEventLoop)` | 给 AcCmUiYieldGate 增加同步判定（如 shouldYield()/tryYield()），仅在预算到期时才 await；否则跳过 await 直接进入下一个实体。 | 否 |
| P3-12 | AcDbBlockTable.removeEntity 对每个 BTR 重建 Set(ids) 且首个命中即 break，跨 owner 的选择删除不完整 | `cad-viewer/packages/data-model/src/database/AcDbBlockTable.ts` | 91-100（配合 database/AcDbBlockTableRecord.ts:493-519） | P3 | `for (const btr of this.database.tables.blockTable.newIterator()) { if (btr.removeEntity(objectId)) { result = true break } }` | 用 db.getObjectById(id) 取实体后按其 ownerId 直接定位 owner BTR 做单次删除（不再遍历块表）；若保留该 API，则不要 break，并返回真实删除数量。 | 否 |
| P3-13 | 代理图形属性 chunk 的 DataView 越过 chunk 长度读取，且 worldDraw/scanExtents 头部读取未校验剩余字节 | `cad-viewer/packages/data-model/src/misc/proxyGraphic/AcDbProxyGraphic.ts` | 617-624（同类：632-639、646-658、665-668、675-682、689-696、703-708、715-720；头部读取 371-378、322-329） | P3 | `const value = new DataView(data.buffer, data.byteOffset, 4).getUint32( 0, true )` | 统一改为 new DataView(data.buffer, data.byteOffset, Math.min(need, data.length)) 并先判断 data.length；头部读取前判断 index + 8 <= buffer.byteLength，不足时按流结束处理。 | 否 |
| P3-14 | AcDbSymbolTable.add 同名记录直接覆盖且不释放旧句柄，旧对象永久驻留 handleRegistry | `cad-viewer/packages/data-model/src/database/AcDbSymbolTable.ts` | 99-111（对比 object/AcDbDictionary.ts:94-107） | P3 | `const normalizedName = this.normalizeName(record.name) if (normalizedName) { this._recordsByName.set(normalizedName, record) } else { this._unnamedRecords.push(record) }` | set 前取 existing，若存在且不是同一对象则 releaseObjectHandle(existing)（在事务录制时按需记录 remove/append）。 | 否 |
| P3-15 | 每个代理实体绘制都全量重建图层/线型名数组 | `cad-viewer/packages/data-model/src/misc/proxyGraphic/AcDbProxyGraphic.ts` | 290-299（构造点 :281；调用点 entity/AcDbProxyEntity.ts:345、440） | P3 | `this._database.tables.layerTable .newIterator() .toArray() .forEach(layer => this._layers.push(layer.name))` | 把 layer/linetype 名字数组缓存在 database 上或用模块级 WeakMap<AcDbDatabase, {layers, linetypes}> 复用，避免逐实例 toArray。 | 否 |
| P3-16 | AcDbDictionary.has 大写化 key 而 getAt/setAt/remove 使用原始 key，两者结果不一致 | `cad-viewer/packages/data-model/src/object/AcDbDictionary.ts` | 258-260（对比 293-295、82-130、146-153） | P3 | `has(name: string) { return this._recordsByName.has(name.toUpperCase()) }` | 统一 key 归一化：要么 setAt/getAt/remove/has 全部做同一种规范化，要么 has 也用原始 key（并记录大小写敏感性约定）。 | 否 |
| P3-17 | 3DSOLID 线框索引硬编码 Uint16Array，顶点数超 65535 时静默截断画错线 | `cad-viewer/packages/data-model/src/entity/AcDb3dSolid.ts` | 349-357 | P3 | `：351-356 const vertexCount = this._wireframe.length / 3 / const indices = new Uint16Array(vertexCount) / for (let i = 0; i < vertexCount; i++) { indices[i] = i } / return` | 按 vertexCount 选择 Uint32Array（>65535 时）或改用 renderer.lineSegments(buffer, 3) 的无索引重载；阈值与实现放在同一处避免再次硬编码。 | 否 |
| P3-18 | MLeader 每条引线的箭头修剪距离在每帧绘制中重复解析样式并遍历箭头块范围 | `cad-viewer/packages/data-model/src/entity/AcDbMLeader.ts` | 3042-3058 | P3 | `：3053-3058 for (const entity of blockTableRecord.newIterator()) { const extents = entity.geometricExtents ... } / return Math.max(0, (maxX - basePoint.x) * size)；:3032 由 ` | 在实体内缓存一次「箭头块最大 X 偏移 × size」的结果（键为 arrowheadId/arrowSize，实体属性变更时失效），并把 getMLeaderStyle() 结果在同一轮 subWorldDraw 内解析一次后传参复用，避免重复字典查找。 | 否 |
| P3-19 | 宽多段线绘制路径用 toFixed 字符串做 Map 键并对整条采样线做 Math.max 展开 | `cad-viewer/packages/data-model/src/entity/AcDbPolyline.ts` | 1272 | P3 | `：1272 const maxWidth = Math.max(...centerline.map(point => point.width))；:1241-1243 return \${point.x.toFixed(4)}:${point.y.toFixed(4)}\（被 splitWidePolylineProfileAtRevis` | 改为一次 for 循环求最大宽度；revisit 检测用数值量化键（如 (x*1e4\|0) 组合的整数键或按段比较）替代 toFixed 字符串，且只在需要判重时才建键。 | 否 |
| P3-20 | HATCH 导出恒写 0 个种子点，读取侧又整体丢弃，DXF 往返丢失种子点 | `cad-viewer/packages/data-model/src/entity/AcDbHatch.ts` | 1879-1880 | P3 | `：1879-1880 // TODO: Write the number of seed points / filer.writeInt16(98, 0)；:2212-2217 case 98: // Seed point count — skip seed points. 与“Seed points after 98 — ignore.` | 在 dxfIn 中把 98/10(11) 后的种子点存入实体字段，dxfOut 按真实数量写 98 并回写种子点坐标；若暂不实现，至少改为保留读到的原始种子点数组而不是写下 0。 | 否 |
| P3-21 | worker 池成功/错误路径永不释放 worker：pendingTasks.resolve 中调用 releaseWorker 的包装器是死代码 | `cad-viewer/packages/data-model/src/converter/worker/AcDbWorkerManager.ts` | 121-134, 148-170, 248-255 | P3 | `this.pendingTasks.set(taskId, { resolve: result => { clearTimeout(timeout) this.releaseWorker(worker) resolve(result as AcDbWorkerResult<TOutput>)` | 在 messageHandler/errorHandler 内改为取回包装器并调用它（const task = this.pendingTasks.get(taskId); task?.resolve({...})），或在 resolve 前显式 this.releaseWorker(worker)；补一条断言 busyWorkers===0 的回归测试。 | 否 |
| P3-22 | worker 池每个任务注册的 message/error 监听器永不注销（无 removeEventListener） | `cad-viewer/packages/data-model/src/converter/worker/AcDbWorkerManager.ts` | 137-182 | P3 | `worker.addEventListener('message', messageHandler) worker.addEventListener('error', errorHandler)` | 在 messageHandler/errorHandler 的每个出口（resolve、reject、timeout、cleanup）统一 removeEventListener 两个监听器（可用 once:true 处理 error），并把监听器与任务生命周期绑定。 | 否 |
| P3-23 | 分批阈值 minimumChunkSize \|\| 200 中的 200 不可达：实体分批默认实际为 10，字面量三处漂移 | `cad-viewer/packages/data-model/src/dxf/AcDbNativeDxfConverter.ts` | 74, 122, 143（另见 dxf/AcDbDxfDocumentReader.ts:538, 751） | P3 | `const { minimumChunkSize = 10, progress } = options ... entityBatchSize: Math.max(1, minimumChunkSize \|\| 200), ... const chunkSize = Math.max(1, minimumChunkSize \|\| 2` | 统一为命名常量（如 ACDB_ENTITY_BATCH_SIZE=200）并在三处使用 options.minimumChunkSize ?? ACDB_ENTITY_BATCH_SIZE，AcDbDxfDocumentReaderOptions.entityBatchSize 的默认复用同一常量。 | 否 |
| P3-24 | registerHandle 合成句柄可能撞上已保留的真实句柄，且大小写不同 key 输出同一句柄（导出重复 group 5） | `cad-viewer/packages/data-model/src/base/AcDbDxfFiler.ts` | 230-249 | P3 | `registerHandle(key: string) { if (!this._handleMap.has(key)) { ... if (/^[0-9A-F]+$/i.test(key)) { this._handleMap.set(key, acdbDxfKeywordUpper(key))` | 把保留句柄按大写归一化后再入 map 并检测冲突；合成时在“已用句柄集合”中跳过冲突值（例如递增到未占用值），或统一走 AcDbDatabase.generateHandle()。 | 否 |
| P3-25 | AcGeCircArc2d.length 对整圆返回 0（缺少 AcGeCircArc3d 的 closed 分支） | `cad-viewer/packages/geometry-engine/src/geometry/AcGeCircArc2d.ts` | 405-407（对照 AcGeCircArc3d.ts:293-297） | P3 | `get length() { return Math.abs(this.deltaAngle * this.radius) }` | 与 3D 版保持一致：get length() { return this.closed ? TAU * this.radius : Math.abs(this.deltaAngle * this.radius) }。 | 否 |
| P3-26 | 零扫掠圆弧被判为整圆：closed 判定与 transform 把退化 ARC/弧边变成完整圆 | `cad-viewer/packages/geometry-engine/src/geometry/AcGeCircArc3d.ts` | 154-161、501-503、605-607（2D 同源：AcGeCircArc2d.ts:366-370、651-654） | P3 | `if ((endAngle - startAngle) % TAU == 0) { this.startAngle = 0 this.endAngle = TAU } // closed: (Math.abs(this.endAngle - this.startAngle) / Math.PI) % 2 == 0` | 整圆判定要求非零扫掠，例如 const sweep = endAngle - startAngle; if (Math.abs(sweep) > 1e-12 && sweep % TAU === 0) {...}；closed 同样排除 deltaAngle===0 的退化情形，transform 的 closed?TAU 分支一并加非零扫掠条件。 | 否 |
| P3-27 | AcGeArea2d.buildHierarchy 用 Math.random() 选包含性测试点，hatch 环嵌套不可复现 | `cad-viewer/packages/geometry-engine/src/geometry/AcGeArea2d.ts` | 125-141（133 行） | P3 | `box2.containsBox(box) && AcGeGeometryUtil.isPointInPolygon( boundary[AcGeMathUtil.randInt(0, boundary.length - 1)], boundary2 )` | 去掉随机采样，改用确定性且稳健的内点测试（例如取某条边的中点并沿局部内法向微移，或对 bbox 中心做多次射线判定取多数），并对 boundary.length===0 提前返回、对 near-duplicate 顶点先去重。 | 否 |
| P3-28 | AcGeLine3d.atLength 的 flag 语义与文档相反，false 分支返回线段外侧的点 | `cad-viewer/packages/geometry-engine/src/geometry/AcGeLine3d.ts` | 107-122 | P3 | `if (flag) { const direction = this.delta(_vector).normalize() return new AcGePoint3d(this._start).addScaledVector(direction, length) }` | 统一语义：flag 为真时基点取 _end 且方向取 -direction（向段内），flag 为假时基点取 _start 且方向取 +direction；或改名为 atDistanceFromEnd/atDistanceFromStart 并同步文档与测试。 | 否 |
| P3-29 | SVG lineSegments 每段输出一个 <line> 元素并重复整套样式属性 | `cad-viewer/packages/cad-svg-plugin/src/AcSvgLineSegments.ts` | 22-43 | P3 | `lines.push( AcSvgStyleUtil.tag('line', { x1: String(x1), y1: String(y1), x2: String(x2), y2: String(y2), ...strokeAttrs }) )` | 合并为单个 <path>（M x1,y1 L x2,y2 …）或按样式分组输出 polyline/path，属性只写一次。 | 否 |
| P3-30 | cad-svg-plugin README 的 peer 依赖清单与实际 peerDependencies/runtime import 不一致 | `cad-viewer/packages/cad-svg-plugin/README.md` | 27-31（另 35 行） | P3 | `Peer dependencies: - @hy/cad-simple-viewer - @hy/data-model - @mlightcad/mtext-parser` | README 补齐 @mlightcad/mtext-renderer 并修正产物文件名（或让其与 pluginRollupOutput 的 createLibEntryFileName 生成规则一致）。 | 否 |
| P3-31 | GeoJSON Polygon/MultiPolygon 只使用外环，内环（孔洞）被静默丢弃且被 SOLID 填充覆盖 | `cad-viewer/packages/cad-tunnel-plugin/src/geojson/geojsonToEntities.ts` | 498-537 | P3 | `const outer = Array.isArray(coordinates[0]) ? coordinates[0] : coordinates const ring = Array.isArray(outer) ? outer.filter(isPosition) : []` | 遍历全部环：内环同样生成 outline 折线，并按 AcDbHatch 多 loop 语义为每个内环 add 一个 loop（或显式记录“暂不支持孔洞”并 log.warn）。 | 否 |
| P3-32 | 搜索面板的防抖定时器未在组件卸载时清理，面板关闭后仍会全量扫描模型空间 | `cad-viewer/packages/cad-search-plugin/src/ui/SearchPanel.vue` | 82-92 | P3 | `let debounceTimer: ReturnType<typeof setTimeout> \| undefined watch(query, value => { if (debounceTimer !== undefined) { clearTimeout(debounceTimer) } debounceTimer = set` | 在 onUnmounted（或 onBeforeUnmount）中 clearTimeout(debounceTimer) 并置 undefined；如后续要加缓存，可一并把结果与 query 解耦。 | 否 |
| P3-33 | MultiPoint 要素完全不生成名称标注，与使用手册声明的行为不一致 | `cad-viewer/packages/cad-tunnel-plugin/src/geojson/geojsonToEntities.ts` | 611-623 | P3 | `case 'MultiPoint': { const list = geometry.coordinates if (Array.isArray(list)) { for (const position of list) { if (!isPosition(position)) continue if (special) { conver` | 在 MultiPoint 分支为首个有效 position 调用 queueLabel（锚点取 shaftLabelAnchor 或点上方 0.7 字高，与 Point 分支一致），或在文档中明确 MultiPoint 不标注。 | 否 |
| P3-34 | Agent 消息内工具标签 v-for 使用可能重复的 :key | `cad-viewer/packages/cad-agent-plugin/src/ui/AgentChatPanel.vue` | 549-555 | P3 | `<div v-for="toolName in toolParts(message)" :key="toolName" class="cad-agent-tool" >` | 改用 part 的唯一 id 作 key（例如返回 {name, key} 或直接遍历 message.parts 并用 part.toolCallId/索引）。 | 否 |
| P3-35 | jest.config.ts 用 readdir 顺序挑选 store 里的 lodash，单测依赖版本不可复现 | `cad-viewer/jest.config.ts` | 13-27 | P3 | `for (const entry of readdirSync(store)) { const candidate = resolve(store, entry, 'node_modules', name) if (existsSync(candidate)) { return candidate } }` | 不要扫描 .pnpm；在根 devDependencies 显式声明与 lodash-es 同版本的 lodash，再映射为 require.resolve('lodash')；或从 pnpm-lock.yaml 精确解析 4.17.21 的 store 路径并断言版本。 | 否 |
| P3-36 | bootstrap.mjs 用下游示例产物作为「全部包已构建」的代理信号 | `bootstrap.mjs` | 78-84 | P3 | `if (!forceMode && existsSync(join(cadViewerDir, 'packages', 'cad-viewer-example', 'dist'))) { console.log('  ⏭ 已存在构建产物,跳过 (--force 可强制重构建)')` | 改为遍历 workspace 包逐一校验其产物（或直接无条件执行 pnpm build，依赖 nx cache 兜底），并把 expectedFiles 由包清单生成而不是硬编码 3 个路径。 | 否 |
| P3-37 | cad-tools/process_dxf.py 缺少 --out 与 --src 同目录保护，可能原地覆盖源图纸 | `cad-tools/process_dxf.py` | 1239-1248 | P3 | `src_dir = Path(args.src) out_dir = Path(args.out) files = sorted(p for p in src_dir.glob("*.dxf") if p.is_file())` | 启动时 if src_dir.resolve() == out_dir.resolve(): 报错退出（并拒绝 out_dir 是 src_dir 子目录的情形），或显式提供 --allow-in-place 开关才允许原地处理。 | 否 |
| P3-38 | 高风险模块零单测且无覆盖率门槛，CI 不做覆盖率采集 | `cad-viewer/.github/workflows/ci.yml` | 56-57 | P3 | `- name: Run tests run: pnpm test` | 补齐 textSearch/locate、CLI 参数解析与 runHeadless、PDF/SVG 导出的单测；在 jest.config.ts 打开 collectCoverage 并设置 coverageThreshold（先按现有测试的包设一个可达成下限），CI 上传覆盖率产物。 | 否 |
| P3-39 | docs 索引与实际内容不一致：漏列圆弧 LOD 文档、仍称 realdwg-web 联合开发 | `docs/README.md` | 30 | P3 | `针对 95MB / 38 万实体大矿图 DXF 的性能战役，五份文档构成完整生命周期：` | 在 02 表格补上 圆弧LOD与渲染数据采集实施计划.md 一行、把「五份文档」改为六份，并把第 3 行的 realdwg-web 表述改为单一 monorepo（cad-viewer/packages/*）。 | 否 |
| P3-40 | AcTrBatchedLine2.dispose 覆盖基类实现且丢失 batchDisposed 标记 | `cad-viewer/packages/three-renderer/src/batch/AcTrBatchedLine2.ts` | 661-664（基类契约见 AcTrBatchedMixin.ts:1270-1274；消费点 AcTrBatchedGroup.ts:1324） | P3 | `dispose() { this.geometry.dispose() return this }` | AcTrBatchedLine2.dispose() 改为 super.dispose()（基类已 dispose geometry），或至少补 this.userData.batchDisposed = true。 | 否 |
| P3-41 | AcTrBufferGeometryUtil.computeLineDistance 对仍在使用的 geometry 调用 dispose() | `cad-viewer/packages/three-renderer/src/util/AcTrBufferGeometryUtil.ts` | 249-296 | P3 | `// TODO: geometry may be referenced by multiple objects. ... line.geometry.dispose() line.geometry = geometry` | 仅在确实替换成新几何体（toNonIndexed 返回新对象）时 dispose 旧对象，并用 userData/引用计数确认无其它对象共享；geometry === line.geometry 时不要 dispose。 | 否 |
| P3-42 | AcTrEntityPreview.disposeObjectTree 释放了与原对象共享的 geometry | `cad-viewer/packages/three-renderer/src/renderer/AcTrEntityPreview.ts` | 330-339, 167-176, 576-582 | P3 | `const ownsClone = object.parent != null return { drawable: ownsClone ? object.clone(true) : object, ownsClone }` | 让 prepareDrawable 同时克隆叶节点 geometry（或只 dispose 本次 capture 自己创建的 geometry，用标记位区分），与 clonePlacedMTextTemplate 的隔离策略保持一致。 | 否 |
| P3-43 | AcTrHtmlTransientManager 的分层隐藏只做一次性遍历，之后新增/reattach 的覆盖层会忽略隐藏状态 | `cad-viewer/packages/three-renderer/src/html/AcTrHtmlTransientManager.ts` | 401-433, 265-274, 588-600 | P3 | `for (const entry of this.entries.values()) { if (this.groupChildIds.has(entry.id)) continue if (entry.layer === layer) { entry.object.visible = visible } }` | 在管理器内维护 hiddenLayers:Set<string>（或全局 hidden 标志），setVisible(layer) 时同步更新，并在 addElement/addGroup/addCanvas/reattach/applyItemLayoutVisibility 中一并应用。 | 否 |
| P3-44 | AcTrBufferGeometryUtil.apply2dTransform 非旋转分支的 Y 坐标公式错误（自累加） | `cad-viewer/packages/three-renderer/src/util/AcTrBufferGeometryUtil.ts` | 342-350 | P3 | `array[i] = array[i] * scale + translation.x // Update x coordinate array[i + 1] += array[i + 1] * scale + translation.y // Update y coordinate` | 改为 array[i + 1] = array[i + 1] * scale + translation.y，并补一条 scale≠1 且 translation.y≠0 的单测锁定行为。 | 否 |
| P3-45 | AcTrVersionManager 版本号硬编码且未被导出/无任何调用者，版本兼容清理形同虚设 | `cad-viewer/packages/three-renderer/src/AcTrVersionManager.ts` | 1-31 | P3 | `const VERSION = '1.0.0' const versionKey = 'VERSION'` | 版本从构建期注入（define/import.meta 或 package.json version）而非字面量；要么在 index.ts 导出并由启动流程真正消费，要么删除该模块；localStorage 访问加 try/catch 与存在性判断。 | 否 |
| P3-46 | 动态 i18n key 违反仓库自身 ESLint 规则，cad-viewer-example lint 无法通过 | `cad-viewer/packages/cad-viewer-example/src/shell/panels/AntdPropertiesPanel.vue` | 108 | P3 | `return t('entity.entityName.' + type, type, { missingWarn: false })（eslint: ''entity.entityName.'+type' dynamic key is used'）` | 本行改用 cad-viewer 已导出的 entityName(entity) 包装（内部走 i18n.global.t，与 entityPropName/entityPropEnum 一致）；ribbon 的 tab/group/button/file/qat 动态 key 改为静态映射表或 te(key) ? t(key) : fallback 统一封装并加 eslint  | 否 |
| P3-47 | useDocument 轮询无上限且通过类型断言直读私有 _instance | `cad-viewer/packages/cad-viewer/src/composable/useDocument.ts` | 289-295 | P3 | `retryTimer = setInterval(() => { tryBind() }, 50) … const singleton = AcApDocManager as unknown as { _instance?: AcApDocManager }（:150-155）` | 加最大重试次数（对齐 useUndoRedo 的 MAX_BIND_RETRIES）并在超限时告警；改用公开就绪接口（如 hasInstance 或 createInstance 的返回值/就绪事件）替代 as unknown as 直读 _instance。 | 否 |

---

说明：P0 为空（14 个领域中没有任何一条被验证者修正为 P0）。完整证据链、验证者复核记录与被推翻条目清单见 `代码评审报告-2026-09-10.md`、`sections/A-严重问题.md`、`sections/B-性能与内存.md`、`sections/C-正确性与工程化.md`。
